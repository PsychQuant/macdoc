## 1. Phase 1 — ocr-swift@0.2.0: new OpenAIBackend + URLProtocol-mocked test coverage

- [x] 1.1 Locally clone `PsychQuant/ocr-swift` to `packages/ocr-swift/` for development; verify it's a git workdir with `origin` pointing at the PsychQuant remote.
- [x] 1.2 Created `Sources/OCRCore/Backends/OpenAIBackend.swift` with `OpenAIBackendError` enum (4 cases: connectionFailed, invalidResponse, serverError(code,body), invalidHost) matching `OllamaBackend` style. Defined `public struct OpenAIBackend: OCRBackend, Sendable`.
- [x] 1.3 Implemented `processImage` with exact JSON shape (model, messages [text + image_url data URL], max_tokens=4096, temperature=0.0, stream=false). Injected `URLSession` parameter for test mockability. Authorization header only when apiKey non-empty. URL constructed from `host + endpoint`.
- [ ] 1.4 [P] Create `Tests/OCRCoreTests/OpenAIBackendTests.swift` per design decision "Backend tests via `URLProtocol` mock (no model downloads)" — use a `URLProtocol` subclass to intercept requests — test cases: successful 200 response parses to expected text, 500 error throws `.serverError(500, body)`, 400 error throws `.serverError(400, body)`, malformed JSON throws `.invalidResponse`, invalid host throws `.invalidHost`, exact URL construction matches `http://<host><endpoint>`, request body JSON shape contains `model`, `messages[0].content` with text + image_url items, `max_tokens=4096`, `temperature=0.0`, `stream=false`, Authorization header present only with non-empty apiKey. Covers the OpenAIBackend URL construction test scenario plus error paths.
- [ ] 1.5 [P] Create `Tests/OCRCoreTests/OllamaBackendTests.swift` mirroring the URLProtocol mock pattern for existing `OllamaBackend` — honours design decision "Keep `OllamaBackend` for native `/api/generate` path" by adding regression coverage for the Backend test coverage via URLProtocol mock requirement. Assert URL is `http://<host>/api/generate`, body shape (`model`, `prompt`, `images` array with single base64 entry, `stream: false`), and at least one error path (non-2xx). Implements the Ollama backend regression coverage scenario.
- [ ] 1.6 Update `Sources/OCRCore/Backends/MLXBackend.swift` docstring per the MLXBackend remains opt-in with documented upstream dependency requirement: add a file-level block documenting `ml-explore/mlx-swift-lm#191` as a known unresolved upstream issue, note that 2.31.3 vs 3.31.3 diffs of `GlmOcr.swift` and `Qwen3VL.swift` are import-only, and recommend `OpenAIBackend` pointed at LM Studio / oMLX / Ollama as production path. No code changes, docstring only.
- [ ] 1.7 Run `swift test` in `packages/ocr-swift/`; all three test files pass (1 existing `PDFKitExtractorTests` plus 2 new backend test files); commit with message `feat: OpenAIBackend + URLProtocol-mocked test coverage (#66)`; per design decision "Release as `ocr-swift@0.2.0`" tag `v0.2.0`; push origin main + tag.

## 2. Phase 2 — macdoc wiring: AIConfig default + MacDoc+OCR / MacDoc+PDF routing + help text

- [ ] 2.1 Per design decision "Default to `OpenAIBackend` pointing at `localhost:11434`": in `packages/pdf-to-latex-swift/Sources/PDFToLaTeXCore/AIConfig.swift`, change the stored-property default and `decodeIfPresent` fallback for `ocrDefaultBackend` from `"ollama"` to `"openai"`, and add inline comment referencing macdoc#66 + the 2026-04-21 decision. Bump `PsychQuant/pdf-to-latex-swift` to a new patch tag (verify current latest first; aim for `v0.1.x+1`) because it owns the AIConfig struct; tag + push.
- [ ] 2.2 In `macdoc/Package.swift`, bump `ocr-swift` pin from `from: "0.1.0"` to `from: "0.2.0"` and (if 2.1 landed) `pdf-to-latex-swift` pin to the new tag; run `swift package update` + `swift build` locally; expect clean build with no compile errors from the new `OpenAIBackend` public symbol.
- [ ] 2.3 In `Sources/MacDocCLI/MacDoc+OCR.swift`, add `case "openai":` to the backend-resolution switch between `"ollama"` and `"mlx"`; construct `OpenAIBackend(host: config.resolveOCRHost(host), model: modelName, apiKey: nil, endpoint: "/v1/chat/completions")` analogous to the Ollama case; update the stderr log line to announce "使用 OpenAI-compat 後端（<host>, 模型: <model>）".
- [ ] 2.4 In `Sources/MacDocCLI/MacDoc+OCR.swift`, update the `--backend` flag help text to list all three choices (`openai` / `ollama` / `mlx`) and clarify that `openai` does NOT require an OpenAI API key but works with LM Studio, oMLX, or any OpenAI-compatible local server.
- [ ] 2.5 Per design decision "Unify `pdf ocr` with top-level `ocr` via AIConfig": in `Sources/MacDocCLI/MacDoc+PDF.swift`, change line 203's `@Option var model: String = "EZCon/GLM-OCR-8bit-mlx"` to `@Option var model: String? = nil`; resolve the effective model via `model ?? config.ocrDefaultModel` (with the same `"glm-ocr"` → `"mlx-community/Qwen3-VL-4B-Instruct-4bit"` compatibility shim when the MLX branch is chosen). This implements the pdf ocr and top-level ocr subcommands share backend resolution requirement.
- [ ] 2.6 In `Sources/MacDocCLI/MacDoc+PDF.swift`, replicate the three-way backend switch (openai / ollama / mlx) so that `macdoc pdf ocr` routes to the same three backends that top-level `ocr` uses, matching the pdf ocr default matches top-level ocr default and pdf ocr explicit override scenarios.
- [ ] 2.7 In `Sources/MacDocCLI/MacDoc+Config.swift`, update the `config ocr set backend` subcommand validator to accept `"openai"` as a valid third value alongside `"ollama"` and `"mlx"`; update the help text accordingly.

## 3. Phase 3 — macdoc tests + docs + release-flow audit

- [ ] 3.1 Add a new smoke test case to `Tests/MacDocCLITests/` (extend existing OCR-related test file or add `OCRNoServerErrorTests.swift`): run `macdoc ocr test.png` with no local server listening on `localhost:11434`; assert that the exit code is non-zero AND the stderr output contains substrings `"localhost:11434"` AND at least one of `"Ollama"`, `"LM Studio"`, `"oMLX"` (diagnostic-quality error message per the Risks/Mitigation section of design). `XCTSkip` when any local server IS responding on port 11434 (detected via a brief TCP probe) to keep the test hermetic in dev environments that happen to run a server.
- [ ] 3.2 [P] Update `macdoc/CLAUDE.md` OCR section: replace any Ollama-only guidance with a three-option table (OpenAI-compat default, Ollama native, MLX opt-in) including port defaults (11434 / 1234 / 8000), one example `macdoc ocr image.png --backend <x>` per option, and a note that the default `openai` backend at port 11434 works with existing Ollama installs unchanged.
- [ ] 3.3 [P] Update `macdoc/SECURITY.md` "Private-repo policy" or add a new "Local OCR servers" subsection: list the three supported server ports (11434 / 1234 / 8000) as loopback-only HTTP surfaces, note that no macdoc setting exposes these ports externally, recommend users keep them bound to 127.0.0.1 (not 0.0.0.0) when running their chosen server.
- [ ] 3.4 Run `./scripts/audit-security.sh macdoc ocr-swift pdf-to-latex-swift` to confirm the `#80 psychquant-security-defaults` baseline remains green across the three repos touched by this change. Document the command + output in the PR description.
- [ ] 3.5 Open a PR against `macdoc main` titled `fix: OpenAI-compat OCR backend (#66)`; PR body SHALL reference this Spectra change directory, the 3 tagged upstream commits (ocr-swift v0.2.0 + pdf-to-latex-swift bump), the #66 closing path via `/idd-close #66` post-merge, and confirm audit-security.sh output.
- [ ] 3.6 After PR merge: invoke `/idd-close #66` with a Closing Summary pointing at this Spectra change, the ocr-swift v0.2.0 release, the rationale (MLX `#191` upstream blocker + Ollama-specific coupling + hardcoded GLM-OCR default), and the verification steps that now live in `OpenAIBackendTests.swift` + `OllamaBackendTests.swift` + macdoc no-server smoke test. Then `/spectra-archive ocr-openai-compat-backend` to sync the Modified + ADDED delta specs into `openspec/specs/simplified-pdf-ocr/spec.md`.
