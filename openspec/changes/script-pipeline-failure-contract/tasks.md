## 1. Make the shared entry point the single home for both guarantees

- [x] 1.1 Write the failing tests for the new entry-point contract in `packages/ooxml-swift/Tests/OOXMLSwiftTests/ScriptPipelineExecuteTests.swift` before changing any implementation. Behavior under test: an existing file at the output path is refused when overwrite was not permitted and is left byte-identical; a run whose verification fails leaves a pre-existing output file byte-identical and creates no file where none existed; the result names no written path when nothing was published; no temporary package remains in the output directory after a failed run. Verify: all four tests fail against the current implementation for the stated reason, not because of a compile error in the test itself.
- [x] 1.2 Implement the modified requirement "Script execution orchestration is a shared transcode entry point" and the design decision "Move the overwrite gate into the shared entry point": add an overwrite parameter to the entry point in `packages/ooxml-swift/Sources/OOXMLSwift/Transcode/ScriptPipelineExecute.swift`, defaulting to refuse, and reject an existing output path before the script is replayed. Behavior: a caller who does not permit overwriting gets a refusal naming the existing path, and the replay cost is not paid. Verify: the refusal test from 1.1 passes, and a test asserts the refusal happens without the script having been parsed.
- [x] 1.3 Implement the design decision "Write to a temporary path, verify, then move into place": write the rebuilt package to a temporary path inside the output path's own directory, compare it against the reference, and move it onto the output path only when the verdict passes or no reference was supplied. Remove the temporary package on every failure path. Behavior: at every instant the output path holds either its original contents or the new verified contents, never a discarded rebuild. Verify: the two non-destructive tests from 1.1 pass, and the stray-temporary-file test passes.
- [x] 1.4 Implement the design decision "Report no written path when nothing was committed": make the result's written-path field optional and populate it only when a package was placed at the output path. Behavior: a caller reading the result cannot be told a path was written when that path still holds its original bytes. Verify: the written-path test from 1.1 passes, and the existing no-reference test still shows a written path present alongside an absent verdict.
- [x] 1.5 Apply the design decision "Require explicit overwrite for same-path self-verification" to the existing same-path coverage, satisfying the modified requirement "The reference document is read before any output is written". Behavior: the existing test that passes one path as both output and reference now requests overwrite explicitly, and a new test asserts that the same call without that request is refused with the file left unmodified. Verify: both tests pass, and the design decision "Keep the read-before-write ordering requirement even though the temporary write also protects it" is honored by the reference still being read into memory before any write, not merely before the move.
- [x] 1.6 Run the full `ooxml-swift` test suite and confirm no existing test regressed. Behavior: the new parameter's default does not silently break callers inside the package. Verify: `swift test` in `packages/ooxml-swift` reports zero failures; if a build-cache crash appears, remove the stale build database before concluding a real defect exists.

## 2. Release the library so both consumers can adopt it

- [x] 2.1 Release the changed entry point as a new `ooxml-swift` version: bump the version, commit, tag, and push. Behavior: a tagged release exists whose entry point carries the overwrite parameter and the optional written path, so both remote-dependency consumers can resolve it. Verify: the tag is reachable on the remote and a clean resolve of that version builds. This step pushes a tag to a shared remote — confirm with the user before pushing, and confirm the working directory is the `ooxml-swift` clone rather than a sibling repository.

## 3. Bring the MCP face onto the new contract

- [x] 3.1 Bump the `ooxml-swift` pin in `mcp/che-word-mcp/Package.swift` to the released version and resolve. Behavior: the MCP server compiles against the entry point that carries the overwrite parameter. Verify: `swift package update` followed by `swift build` succeeds in `mcp/che-word-mcp`.
- [x] 3.2 Write the failing tests for the MCP contract in `mcp/che-word-mcp/Tests/CheWordMCPTests/ScriptPipelineParityTests.swift` before changing the handler. Behavior under test: a failing verification surfaces as a tool error whose text names each differing part rather than as a success carrying a false verdict; an existing output path is refused when the overwrite parameter was not set, leaving that file unmodified; the response omits the written-path key when nothing was published. Verify: all three tests fail against the current handler for the stated reason.
- [x] 3.3 Implement the design decision "Signal a failed verification as an error, accepting loss of the structured payload" in `mcp/che-word-mcp/Sources/CheWordMCP/ScriptPipelineTools.swift`, satisfying the modified requirement "Script execution rebuilds with verifiable byte equality". Behavior: `execute_script` raises on a failing verdict with the differing parts named in the error text, and returns normally on a passing verdict or when no reference was supplied. Verify: the failing-verification test from 3.2 passes and the existing passing-verification test is unchanged and still passes.
- [x] 3.4 Add the overwrite parameter to the `execute_script` input schema in `mcp/che-word-mcp/Sources/CheWordMCP/Server.swift` and pass it through to the shared entry point, adding no overwrite check of the tool's own. Behavior: the schema documents the parameter as defaulting to refuse, and the tool description no longer states that an existing file is overwritten. Verify: the refusal test from 3.2 passes, and searching `mcp/che-word-mcp/Sources` finds no file-existence check on the output path.
- [x] 3.5 Run the `che-word-mcp` test suite, including the existing parity tests. Behavior: the parity guard still holds now that both faces route through the same gate. Verify: `swift test` in `mcp/che-word-mcp` reports zero failures.

## 4. Bring the CLI face onto the new contract

- [x] 4.1 Bump the `ooxml-swift` pin in the root `Package.swift` to the released version and resolve. Behavior: the CLI compiles against the same entry-point commit the MCP server uses. **This does NOT make the two faces immune to dependency drift, and an earlier draft of this task wrongly claimed it did.** The root's `Package.resolved` pins `ooxml-swift` by *branch*, not by version, because `MacDocCLI` reaches it transitively through `word-builder-swift`, which declares it as `branch: "main"` — and a transitive branch requirement overrides the root's own version range. The two faces agree today only because the branch tip happens to be the released tag. Verify: `swift package update` followed by `swift build` succeeds at the repository root, AND the resolved state is recorded as a divergence risk rather than reported as a guarantee. Durable fix requires cross-repo coordination and is tracked separately.
- [x] 4.2 Write the failing tests for the CLI contract in `Tests/MacDocCLITests/WordRenderTests.swift` before changing the command. Behavior under test: a run whose verification fails leaves a pre-existing output file byte-identical and does not print a completed-write line; the refusal to overwrite still occurs after the local check is removed. Verify: the first test fails against the current command because the write is announced before the verdict is known.
- [x] 4.3 Satisfy the modified requirement "Render command rebuilds a document from a script" in `Sources/MacDocCLI/MacDoc+Word+Render.swift`: pass the overwrite flag through to the shared entry point, delete the command's own file-existence check, and report a completed write only after the verdict is known. Behavior: the user-visible flag spelling and refusal message are unchanged, but the guarantee now originates from one place; a failed verification prints the differing parts and no completed-write line. Verify: the tests from 4.2 pass, and searching `Sources/MacDocCLI` finds no file-existence check on the render output path.
- [x] 4.4 Run the macdoc test suite in both debug and release configurations. Behavior: the CLI test harness selects a freshly built binary rather than a stale one, so the assertions describe the code under change. Verify: `swift test` reports zero failures, and the release binary is rebuilt before any test that invokes it by path.

## 5. Make the documented contract match the shipped one

- [x] 5.1 [P] Satisfy the added requirement "The workflow states what a failed run leaves behind" in `plugins/macdoc/skills/swiftify/SKILL.md` and update the render section of `plugins/macdoc/skills/macdoc/SKILL.md`. Behavior: the workflow states that a failed verification publishes nothing, shows the overwrite request inside its own repeated-render template sequence rather than leaving the operator to hit the refusal, and states that a failing verification on the MCP face arrives as an error rather than as a field to inspect. Verify: following the documented template sequence verbatim produces no refusal, and no sentence in either document describes the rebuilt document as written before verification is reported.
- [x] 5.2 [P] Update the script-pipeline section of `plugins/che-word-mcp/skills/che-word-mcp/SKILL.md` for the new `execute_script` contract. Behavior: the document states the overwrite parameter and its refusing default, states that a failing verification arrives as a tool error naming the differing parts, and states that the written-path key is omitted when nothing was published. Verify: no sentence in the document says an existing output file is overwritten, and the described response shape matches what the handler returns for each of the three cases.
- [x] 5.3 Record the deferred alternative as its own issue against `che-word-mcp`: allow a tool handler to set the error flag while still returning a structured JSON body, so a failing verification can carry both the failure signal and the machine-readable differing-part list. Behavior: the trade-off accepted in the design decision "Signal a failed verification as an error, accepting loss of the structured payload" is tracked rather than silently absorbed, and the issue records that it touches the shared dispatch layer for every tool. Verify: the issue exists, references both originating issues, and is linked from this change's proposal Non-Goals rationale.
