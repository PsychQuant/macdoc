## 1. CLISpec target and deterministic YAML emitter

- [x] 1.1 Add a dependency-free `CLISpec` library target (Sources/CLISpec) to Package.swift and make only the `MacDocCLITests` test target depend on it, per design "One transform shared by the drift test and make cli-spec"; this delivers the "Executable is unaffected" scenario of requirement "Code-first authority and generated-file header". **Verify**: `swift build` succeeds; `git diff --exit-code Package.resolved` is clean; the `MacDocCLI` target's dependency list in Package.swift does not mention `CLISpec`.
- [x] 1.2 Write failing `CLISpecEmitterTests` covering requirement "Deterministic YAML serialization": every row of the `##### Example: Scalar quoting` table, block sequences of mappings indented two spaces under their key, flow sequences of scalars, `notes` as a block sequence of strings, booleans and integers, no trailing whitespace, exactly one trailing newline, header comment lines. **Verify**: `swift test --filter CLISpecEmitterTests` fails to compile or fails (RED) because `YAMLEmitter` does not exist yet.
- [x] 1.3 Implement the ordered `YAMLNode` tree and `YAMLEmitter.emit(_:headerComments:)` per design "Deterministic YAML subset emitter over an ordered node tree" (plain-scalar rule, double-quote escapes, LF, final newline). **Verify**: `swift test --filter CLISpecEmitterTests` passes (GREEN).

## 2. Dump-help decoding and builder

- [x] 2.1 Write failing `CLISpecBuilderTests` for requirement "Dump-help input is isolated behind a versioned decoder" using synthetic in-test JSON: serializationVersion 1 → `unsupportedSerializationVersion(1)`; extra `futureField` keys → byte-identical output; every row of the `##### Example: Version and parsing-strategy spellings` table; an unknown `kind` → `malformedDump`. Then implement the private `Decodable` V0 types and the version guard per design "Out-of-process dump-help consumed through a private versioned decoder". **Verify**: `swift test --filter CLISpecBuilderTests` goes RED then GREEN.
- [x] 2.2 Write failing then passing builder tests for requirements "Derived command surface and ordering", "Builtin surface normalization" and "Argument field mapping" on a synthetic three-level dump: depth-first pre-order in declaration order, `default_subcommand`, `subcommands`, dropped `--help` / `-h` / `-help` / `--version` flags and root `help` subcommand, `tool.version` from the version output, names ordered long → single-dash → short, `required` always present, `repeating` / `hidden` only when true, `parsing`, `default`, `values`, `section`, `help`, per design "Builtin surface normalization and declaration-order traversal" and "Project-owned schema with a derived/overlay provenance split". **Verify**: `swift test --filter CLISpecBuilderTests` passes and asserts the exact emitted YAML for the synthetic tree.
- [x] 2.3 Write failing then passing builder tests for requirement "Overlay cross-reference validation": one test per failure class (`unknownCommandPath`, `duplicateCommandMetadata`, `unknownOption`, `unknownStyle`, `duplicateConversion` by label and by extension pair, `unknownDependency`, `duplicateDependency`, `unusedDependency`), each asserting the thrown case and that its description names the offending value; plus the derived `used_by` order (commands first, then conversion labels, no duplicates) from requirement "Project metadata overlay". **Verify**: `swift test --filter CLISpecBuilderTests` passes.
- [x] 2.4 Add the determinism test for requirement "Deterministic YAML serialization": generate twice from the same synthetic inputs and once from the same dump re-serialized with sorted keys; all three byte-identical. **Verify**: `swift test --filter CLISpecBuilderTests` passes.

## 3. Overlay, contract tests and the committed specification

- [x] 3.1 Author `MacDocCLIMetadata.overlay` per design "Metadata overlay as a compiler-checked Swift literal" and requirement "Project metadata overlay": `removed` status with bestOCR replacement for `macdoc ocr`; `deprecated` with replacement `macdoc pdf ocr` for `pdf blocks` / `transcribe` / `transcribe-pages` / `resume`; the 19 conversions (17 `convert` conversions, the `tokens` route, and `PDF → LaTeX` via `macdoc pdf`) with extensions, targets, converters, output modes, route-specific options, styles, dependencies and notes read from the `convert` implementation; overlaps for PDF extraction/OCR paths and BibLaTeX paths; the external-dependency catalog (playwright, tex, ai-cli, ollama, huggingface, anthropic-api, microsoft-word). **Verify**: building the document from the real binary's dump throws no `CLISpecError` (exercised by 3.2).
- [x] 3.2 Add the test-side harness (resolves the binary through `CLITestHelper.binaryPath`, runs `macdoc --experimental-dump-help` and `macdoc --version` while draining both pipes — the ~180 KB dump exceeds one pipe buffer — and calls `CLISpecGenerator.generate`) and `CLISpecContractTests` asserting the scenarios of requirements "Derived command surface and ordering", "Builtin surface normalization", "Argument field mapping" and "Project metadata overlay" against the real binary: root subcommands, the `macdoc convert` `--to` / `--css` YAML blocks verbatim, `--profile` values, the eight-path order prefix and `macdoc pdf ocr` placement, `macdoc word reverse` `--slot` and `macdoc word render` fields, the `macdoc config document` subtree, the `macdoc ocr` shim (hidden `ignored` positional, `removed` status), deprecated pdf commands, the PDF overlap, and `playwright` `used_by`. **Verify**: `swift test --filter CLISpecContractTests` passes.
- [x] 3.3 Add `CLISpecDriftTests` and the Makefile target `cli-spec` per requirement "Drift contract for the committed specification": byte comparison against the repository-root `cli-spec.yaml`, failure message with first differing line number, both lines and `make cli-spec`; record mode only for `MACDOC_RECORD_CLI_SPEC=1`. Generate and commit `cli-spec.yaml`, whose header satisfies requirement "Code-first authority and generated-file header". **Verify**: `make cli-spec` writes the file; `swift test --filter CLISpecDriftTests` then passes; temporarily altering one line of the committed file makes it fail with that line number, and restoring it passes again.

## 4. Consistency checks against the binary and CONVERSIONS.md

- [x] 4.1 [P] Add `CLISpecRouteProbeTests` per requirement "Convert routes match the binary's dispatch" and design "Route probe against the binary's convert dispatch": derive E × T from the overlay's `macdoc convert` conversions (excluding `"*"`) unioned with the fixed probe vocabulary, run each pair on an empty probe file in a temporary directory with `PATH=/usr/bin:/bin`, assert listed pairs never print `不支援從` and unlisted pairs exit non-zero with `不支援從 .<ext> 轉換到 <target>`, including every row of the `##### Example: Probe expectations` table. **Verify**: `swift test --filter CLISpecRouteProbeTests` passes.
- [x] 4.2 [P] Add the missing Converter Details rows (Word → Marker, HTML → PDF, LaTeX → Word) and the matching cross-matrix cells to CONVERSIONS.md, and add `CLISpecConversionsDocTests` per requirement "CONVERSIONS.md agrees with the specification" and design "CONVERSIONS.md consistency by label equality": parse the table under `## Converter Details`, compare label sets with the specification's conversions, and report one-sided labels; a unit case feeds a table without `HTML → PDF` and asserts the failure names it. **Verify**: `swift test --filter CLISpecConversionsDocTests` passes.

## 5. Documentation and final verification

- [ ] 5.1 Point CLAUDE.md at the generated specification per design "Code-first authority with a generated cli-spec.yaml": a Development Commands entry for `make cli-spec` and the drift test, and a Key Files entry for `cli-spec.yaml` and `Sources/CLISpec/MacDocCLIMetadata.swift`. **Verify**: content review — both entries present and name the regeneration command.
- [ ] 5.2 Run the full suite and Spectra validation. **Verify**: `swift test` reports zero failures with the baseline counts (111 XCTest with 4 skipped, 43 Swift Testing) plus the new CLISpec tests; `spectra validate cli-spec-yaml` reports valid; `git diff --exit-code Package.resolved` is clean.
