## 1. Test infrastructure

These tasks land the directory structure, normalizer, and test runner. They MUST land before any fixture pair is authored — the runner enforces the file-set contract during fixture validation.

- [x] 1.1 Create the corpus root `packages/ooxml-swift/Tests/OOXMLSwiftTests/Fixtures/mdocx/` with subdirectory `_normalizer/` containing `word-default-theme.xml` (vendor-copy of the canonical Microsoft Word default theme XML). This file is referenced by the normalizer to detect default-theme stripping per the **Normalization pipeline behavior** requirement
- [x] 1.2 [P] Implement `packages/ooxml-swift/Tests/OOXMLSwiftTests/MdocxFixtureNormalizer.swift` per Decision 2: Normalize before byte-diff; normalizer is part of the test infrastructure. The implementation MUST satisfy the **Normalization pipeline behavior** requirement (idempotent, deterministic, pure; strips RSIDs + default theme + default settings keys + re-numbers UUID-shaped IDs to monotonic sequence preserving cross-references). Add `MdocxFixtureNormalizerTests.swift` covering each strip rule with explicit input / expected-output pairs. Also documents the **Hand-crafted golden source** requirement contract (since the normalizer is the bridge between the hand-crafted raw form and the byte-diff target)
- [x] 1.3 [P] Implement `packages/ooxml-swift/Tests/OOXMLSwiftTests/MdocxFixtureCorpusTests.swift` per Decision 3: Fixture directory numbering tracks `mdocx-grammar` Requirement order, with letter suffix for variants and Decision 4: Minimum file set per fixture pair, with optional additions for special Requirements and Decision 5: Phase-aware test runner — Phase A enforces structure, Phase B activates execution after WordDSLSwift implementation lands. The runner MUST enforce: the **Corpus completeness contract** (every mdocx-grammar Requirement has at least one fixture, identified by NN prefix), the **Fixture directory layout and naming** requirement (NN[letter]-slug pattern), the **Minimum file set per fixture pair** requirement (.mdocx.swift + .docx + .normalized.docx + README.md), the **Optional file additions for special Requirements** requirement (.oplog.jsonl + .snapshot.json + .expected-source.mdocx.swift gated by Requirement number), and the **Phase-aware parameterized test runner contract** (Phase A always-on: layout + file-set + .mdocx.swift compile-pass + completeness; Phase B gated by `activatePhaseB` flag: script execution + docx byte-diff + op-log + snapshot + reverse-CLI). The Phase B test code path is shipped fully but inactive (`activatePhaseB = false` default), ready for the WordDSLSwift-implementing change to flip the flag
- [x] 1.4 Validate the Phase A infrastructure end-to-end with one smoke fixture (the simplest one, `01-dual-extension-recognition`): hand-craft the docx, compute the normalized form, write the `<slug>.mdocx.swift` source, write the README, run the runner with `activatePhaseB == false`, confirm Phase A assertions pass (layout + file-set + .mdocx.swift Swift compile-pass against current WordDSLSwift placeholder); then deliberately break the fixture (delete README, rename file, introduce a Swift syntax error in .mdocx.swift) and confirm the runner produces clear diagnostics naming the failed Phase A assertion

## 2. Fixture pairs covering core grammar Requirements

Each task in this group authors 2-3 fixture pairs for related mdocx-grammar Requirements. Per Decision 1: Hand-crafted XML for golden docx, not Word-saved or ooxml-swift-emitted, every `<slug>.docx` SHALL be authored by hand and reviewed for absence of RSID / Word-version-specific noise. Each fixture SHALL satisfy the **Hand-crafted golden source** requirement and pass the runner's file-set + naming validation. Tasks in this section are mutually independent (different directories, no file overlap) and execute in parallel.

- [x] 2.1 Author fixtures 01-dual-extension-recognition (covers `mdocx-grammar` Requirement "File extension and dual-extension pattern"), 02a-plain-string and 02b-explicit-run-multi-format (both cover Requirement "Flat Run with implicit String literal inline grammar"). For each: hand-craft `<slug>.docx`, run normalizer to produce `<slug>.normalized.docx`, write `<slug>.mdocx.swift`, write README naming the Requirement and edge case
- [ ] 2.2 [P] Author fixtures 03-tab-break-nbhyphen (covers Requirement "Special-character inline atoms as standalone children"), 04-mirrored-element-set (covers Requirement "OOXML-mirror element naming"), 05-heading-via-style (covers Requirement "No semantic shortcuts for OOXML-style attributes"). Same per-fixture procedure as 2.1
- [ ] 2.3 [P] Author fixtures 06-two-sections-marker-inversion (covers Requirement "Section as DSL container with compile-time marker inversion"; the docx contains two sections demonstrating the OOXML marker pattern with `<w:sectPr>` placement), 07-summary-component (covers Requirement "Component-aware op log via BeginComponent and EndComponent"; this fixture also includes the optional `summary-component.oplog.jsonl` per the **Optional file additions for special Requirements** requirement)
- [ ] 2.4 [P] Author fixtures 08-explicit-id-everywhere (covers Requirement "Mandatory explicit identifiers on structural elements"; every Section, Paragraph, WordComponent, Bookmark, Hyperlink-as-anchor-target, Table, TableRow, TableCell instance in the .mdocx.swift carries an explicit `id:` argument), 09-style-define-on-first-use (covers Requirement "Style references via typed enum with define-on-first-use"; two paragraphs reference the same `WordStyle` and the optional `<slug>.oplog.jsonl` shows exactly one `DefineStyle` op preceding both `InsertParagraph` ops)
- [ ] 2.5 [P] Author fixtures 10a-table-1x1 and 10b-table-3x3-with-formatting (both cover Requirement "Table grammar mirrors OOXML three-layer structure"; the 1×1 minimal case + the 3×3 with cell formatting), 11-bullet-and-numbered-lists (covers Requirement "Lists use Paragraph with numPr reference, not nested containers"; includes a numbered list, a bullet list, and a nested-level item)
- [ ] 2.6 [P] Author fixtures 12-hyperlink-url-anchor-mailto (covers Requirement "Hyperlinks are containers with target enum"; one fixture with three hyperlinks of types `.url`, `.anchor`, `.mailto` to demonstrate the typed enum surface), 13a-bookmark-container and 13b-bookmark-cross-paragraph (both cover Requirement "Bookmarks default to container with paired-marker escape hatch"; the container form spanning a single Run + the paired-marker form spanning two paragraphs)

## 3. Fixture pairs for special-shape Requirements

These two Requirements do not have a single docx golden; they exercise process-level (atomic save) or CLI-level (reverse) behavior. Both fixtures use the optional file additions per the **Optional file additions for special Requirements** requirement.

- [ ] 3.1 Author fixture 14-atomic-three-file-save (covers `mdocx-grammar` Requirement "save(to:) atomic three-file write"). The fixture includes `.mdocx.swift` ending with `try doc.save(to: ...)`, the expected `<slug>.docx`, the expected `<slug>.docx.oplog.jsonl`, the expected `<slug>.docx.snapshot.json` (the snapshot triple per the **Optional file additions for special Requirements** Requirement 14 path), plus a README documenting the failure-case assertion (forcing a write error on file 2 leaves files 1 and 3 unchanged)
- [ ] 3.2 [P] Author fixture 15-reverse-cli-roundtrip (covers `mdocx-grammar` Requirement "Reverse CLI shape — macdoc word reverse"). The fixture includes `input.mdocx.swift` (executed to produce a docx), `<slug>.docx`, `<slug>.normalized.docx`, plus the optional `expected-source.mdocx.swift` (the source `macdoc word reverse` SHALL produce when run against the produced docx, used per the **Optional file additions for special Requirements** Requirement 15 path) plus a README documenting the canonicalization rules (whitespace + parameter order normalization) used in the source equivalence check

## 4. Phase A verification (this change)

- [ ] 4.1 Run `MdocxFixtureCorpusTests` against the full corpus with `activatePhaseB == false`; assert 18 parameterized Phase A test cases execute and pass (layout + file-set + .mdocx.swift Swift compile-pass + completeness); verify the **Corpus completeness contract** check reports green coverage for all 15 mdocx-grammar Requirements; record any drift between the spec and the corpus, and either correct the corpus or open a follow-up `/spectra-ingest mdocx-grammar` to amend the spec

## 5. Phase B activation hand-off (deferred — happens in `word-aligned-state-sync` Phase 4 or its successor)

These tasks DO NOT execute within this change. They are recorded here so the future change that lands the `WordDSLSwift` module implementation knows the activation contract per Decision 5. When that change is being authored, copy these tasks into its `tasks.md` and execute them there.

- [ ] 5.1 (deferred) Confirm fixture `01-dual-extension-recognition` Phase B assertions pass against the new `WordDSLSwift` implementation: `<slug>.mdocx.swift` executes, produces a docx, normalizer transforms it, byte-equals the `<slug>.normalized.docx` golden
- [ ] 5.2 (deferred) Flip `MdocxFixtureCorpusTests.activatePhaseB = true` and run the full suite; investigate every fixture failure and either fix the fixture, fix the WordDSLSwift implementation, or open `/spectra-ingest mdocx-grammar` if the spec needs amendment
- [ ] 5.3 (deferred) Update `openspec/specs/mdocx-fixture-corpus/spec.md` (or this change's archived design.md if accessible) with a "Phase B activated by change `<change-name>` on `<date>`" footnote so future readers know when the gate flipped
