## 1. Promote execute orchestration into the shared transcode module

- [x] 1.1 Implement the requirement "Script execution orchestration is a shared transcode entry point" and the design decision "Promote execute orchestration into the shared transcode module": move the execute orchestration and its result type from `mcp/che-word-mcp/Sources/CheWordMCP/ScriptPipelineTools.swift` into `packages/ooxml-swift/Sources/OOXMLSwift/Transcode/ScriptPipelineExecute.swift` as a public entry point. Behavior: a caller holding only the OOXMLSwift module can execute a rebuild script and receive the written path, an optional verdict, and the differing-part list; the verdict is absent, not false, when no reference was supplied. Verify: a new OOXMLSwift-level test executes a script with and without a reference and asserts the verdict is present in the first case and absent in the second.
- [x] 1.2 Implement the requirement "The reference document is read before any output is written", realizing the design decision "The write-ordering constraint becomes normative": the reference document's parts are read into memory before the rebuilt package is written. Behavior: supplying one path as both output and reference yields a verdict computed against the file's pre-write contents, so a self-comparison can never manufacture a passing result. Verify: a test passes the same path as output and reference for a script that alters content, and asserts the verdict fails rather than passes.
- [x] 1.3 Satisfy the modified requirement "Script execution rebuilds with verifiable byte equality" by reducing the che-word-mcp `execute_script` handler to argument parsing over the promoted entry point, deleting the local orchestration copy. Behavior: the tool's JSON response is byte-identical to its pre-change output for the same inputs, including omission of verdict fields when no reference is passed. Verify: run the existing `ScriptPipelineParityTests` and the execute-tool tests in `mcp/che-word-mcp/Tests/CheWordMCPTests/` and confirm they pass unmodified.

- [x] 1.4 Add the cross-face agreement test required by the modified requirement "Script execution rebuilds with verifiable byte equality". Behavior: one script and one reference produce the same verdict and the same differing-part set through the MCP `execute_script` tool and through `macdoc word render`, which is what calling one shared entry point is supposed to guarantee. Verify: a test drives both faces over identical inputs and asserts the two results match.
- [x] 1.5 Release the promoted API as `ooxml-swift` v2.1.0. Behavior: a tagged release exists whose `OOXMLSwift` module exports the script execution entry point, so remote-dependency consumers can resolve it; the version is a minor bump because the change adds public API without removing any. Verify: `git -C packages/ooxml-swift describe --tags --abbrev=0` reports the new tag and `swift build` succeeds inside that package. This step pushes a tag to a shared remote — confirm with the user before pushing.
- [x] 1.6 Bump the `ooxml-swift` dependency pin in the root `Package.swift` and in `mcp/che-word-mcp/Package.swift` to the released version, then resolve. Behavior: both consumers compile against the promoted entry point rather than the stranded copy; the root pin moves off its current `1.4.0` floor and che-word-mcp moves off `2.0.1`. Verify: `swift package update` followed by `swift build` succeeds in both the repository root and `mcp/che-word-mcp`.

## 2. Add the render command to the CLI

- [x] 2.1 Implement the requirement "Render command rebuilds a document from a script", applying the design decision "Name the CLI subcommand `render`, not `execute`". Add a `Render` subcommand under `macdoc word` in `Sources/MacDocCLI/MacDoc+Word+Render.swift`, registered in `Sources/MacDocCLI/MacDoc+Word.swift`, calling the promoted entry point. Behavior: `macdoc word render <script> --to-docx <out>` writes a rebuilt document and reports the written path on stderr following the convention `word reverse` already uses. Verify: `macdoc word render --help` lists the command, and a test asserts the output file exists after a round trip.
- [x] 2.2 Accept both the dual-extension and the bare `.mdocx` script filename forms. Behavior: a script saved without the trailing `.swift` renders identically to one saved with it, satisfying the dispatch requirement already stated in `mdocx-grammar`. Verify: a test renders the same script content under both filenames and asserts identical output bytes.
- [x] 2.3 Implement the design decision "Verification is opt-in and off by default" via a reference-document option. Behavior: naming a reference turns on byte-equal comparison and a mismatch exits non-zero naming the differing parts; omitting it writes the output, prints no verdict, and exits zero. Verify: tests cover three cases — matching reference exits zero, altered reference exits non-zero and names a part, no reference prints no verdict.
- [x] 2.4 Guard inputs and outputs. Behavior: a missing script or missing reference is refused before anything is written, naming the path that was not found; an existing output file is refused unless overwrite is requested, leaving the existing file unmodified. Error text is Traditional Chinese per this repository's CLI conventions. Verify: tests assert no output file is created on the missing-input paths and that the pre-existing file is unchanged on the refusal path.
- [x] 2.5 Add a round-trip test over the raw-channel case in `Tests/MacDocCLITests/WordRenderTests.swift`. Behavior: a document whose coverage report shows a zero DSL ratio for every part still reverses and renders back to a byte-equal document, confirming the raw channel carries the byte-equal floor. Verify: the test reverses a table-bearing fixture, renders it with the original as reference, and asserts a passing verdict; it skips rather than fails when the fixture is unavailable, following the existing fixture-detection pattern in `Tests/MacDocCLITests/CLITestHelper.swift`.

## 3. Bring the specs in line with the code

- [x] 3.1 Replace the placeholder Purpose in `openspec/specs/ooxml-script-transcode/spec.md`, which still carries the archive-time placeholder text left by `word-aligned-state-sync`. Behavior: the spec states what the transcode capability covers, so a reader arriving at the promoted entry point's requirements has context. Verify: read the file and confirm no placeholder or archive-stub text remains.
- [x] 3.2 Confirm the delta specs in this change match what was built, adjusting any requirement whose wording drifted during implementation. Behavior: every scenario in the four delta specs corresponds to a test or a command that exists. Verify: run `spectra validate script-pipeline-surface` and walk each scenario against its named test.

## 4. Document the pipeline on the skill surfaces

- [x] 4.1 [P] Add the `word` subcommand group to the macdoc plugin skill in `plugins/macdoc/skills/macdoc/SKILL.md`, covering `word reverse` and `word render`. Behavior: the skill's subcommand table lists `word`, which it currently omits entirely, and a reader can complete a round trip from the command line using only this document. Verify: confirm the table contains a `word` row and that every command shown runs successfully as written.
- [x] 4.2 [P] Add the three script-pipeline tools to `plugins/che-word-mcp/skills/che-word-mcp/SKILL.md`. Behavior: `export_script`, `get_script_coverage`, and `execute_script` are documented with their parameters, and the document names `macdoc word render` as the CLI counterpart of `execute_script`. Verify: search the file for each tool name and confirm each has parameter documentation.
- [x] 4.3 Implement the requirement "Coverage check precedes expectation of readable output" across both skills from 4.1 and 4.2, using the measured result rather than a general caution. Behavior: each document states that DSL upgrade is decided per part on an all-or-nothing basis, that a document containing a table drops its whole main part to the raw channel, and that a real NTU-REC form measures a zero DSL ratio across all sixteen parts. Verify: confirm both documents contain the measured figure and state the boundary as a non-promise rather than an exception.
- [x] 4.4 Create the workflow skill at `plugins/macdoc/skills/swiftify/SKILL.md`, per the design decision "`swiftify` is a workflow skill inside the macdoc plugin", implementing the requirement "The workflow covers the complete round trip on both faces" and the requirement "The workflow states its fidelity guarantee and its non-promise". Behavior: the document walks export, coverage reading, optional slot substitution, render, and verification, naming both the CLI entry point and the MCP tool for each step, and states plainly that readable Swift output is not guaranteed for any input. Verify: check each requirement of the `swiftify-workflow` spec against a passage in the document.
- [x] 4.5 Apply the design decision "Division of labor across the three skill documents" so future edits do not re-describe the pipeline in all three. Behavior: the swiftify skill owns the workflow; the other two own their own surfaces as reference. Verify: confirm neither reference document restates the workflow and that the workflow document does not duplicate parameter tables.

## 5. Ship

- [x] 5.1 Bump the macdoc plugin shell version in `plugins/macdoc/.claude-plugin/plugin.json` and mirror it in `.claude-plugin/marketplace.json`. Behavior: both files carry the same new shell version. `binary_version` stays put here because the repository rule moves it only after the binary repo publishes a release — see 5.4, which is what actually delivers `word render` to plugin users. Verify: compare the version field in both files and confirm `binary_version` is unchanged.
- [x] 5.2 Update the `Key Files` entry for `Sources/MacDocCLI/MacDoc+Word.swift` in `CLAUDE.md` to cover `word render` alongside `word reverse`. Behavior: the repository guide describes both halves of the pipeline. Verify: read the entry and confirm it names the render command and its verification option.
- [ ] 5.4 Cut a macdoc CLI release carrying `word render`, then bump `binary_version` in both plugin manifests to match. Behavior: a plugin user whose `session-start.sh` downloads the CLI from `releases/download/v$binary_version` receives a binary that actually has the `word render` subcommand the skills now document; until this lands, those skills describe a command that installed users cannot run. Verify: `macdoc word --help` from a freshly downloaded release binary lists `render`. This publishes a GitHub release — confirm with the user before cutting it.
- [x] 5.3 Run the full test suites and confirm no regressions. Behavior: the macdoc suite and the che-word-mcp suite both pass, with the che-word-mcp suite passing without modification since its tool contract is unchanged. Verify: run `swift test` at the repository root and in `mcp/che-word-mcp`, and report both pass counts.
