## 1. Phase 1 — Document root namespace preservation

- [x] 1.1 Write failing test in `packages/ooxml-swift/Tests/OOXMLSwiftTests/DocumentXmlLosslessRoundTripTests.swift` named `testRootNamespacesRoundTripPreservesAll34Declarations` that builds a `<w:document>` root with 34 `xmlns:*` declarations + `mc:Ignorable`, parses via `DocxReader`, saves via `DocxWriter`, asserts every source attribute appears in output (per spec requirement "WordDocument preserves <w:document> root element attributes byte-equivalent across no-op round-trip"). Test must currently fail.
- [x] 1.2 [P] Add public field `documentRootAttributes: [String: String]` to `packages/ooxml-swift/Sources/OOXMLSwift/Models/Document.swift`, default empty (per design decision "Position-index ordering, not enum refactor" carries over: additive field, no enum churn).
- [x] 1.3 Extend `DocxReader.read(from:)` in `packages/ooxml-swift/Sources/OOXMLSwift/IO/DocxReader.swift` to extract every attribute from the source `<w:document>` root element (including all `xmlns:*` namespace declarations and `mc:Ignorable`) and assign to the new `documentRootAttributes` field, preserving source order.
- [x] 1.4 Modify `DocxWriter.writeDocument(_:to:)` at `packages/ooxml-swift/Sources/OOXMLSwift/IO/DocxWriter.swift:639-659` to rebuild the root open tag from `documentRootAttributes`, falling back to hardcoded `xmlns:w` + `xmlns:r` only when the dictionary is empty (preserving create-from-scratch behavior).
- [x] 1.5 Add scenario test `testCreateFromScratchEmitsMinimalNamespaceSet` verifying `WordDocument()` initializer path still emits exactly two namespace declarations (per spec scenario "Create-from-scratch document emits minimal namespace set"). Verify Phase 1 tests pass.

## 2. Phase 2 — Bookmark Reader parsing + range markers

- [x] 2.1 [P] Write failing test `testBookmarkPairRoundTripsAllAttributes` in `DocumentXmlLosslessRoundTripTests.swift` covering the spec requirement "Bookmark elements round-trip lossless across no-op save" (build paragraph with 3 bookmark pairs at varied positions, assert reader populates `Paragraph.bookmarks`, asserts writer emits them at original positions).
- [x] 2.2 [P] Create `packages/ooxml-swift/Sources/OOXMLSwift/Models/ParagraphChildMarkers.swift` defining `BookmarkRangeMarker { kind: enum (start, end), id: Int, position: Int }` (and reserve file structure for the other 6 marker types added in Phase 4).
- [x] 2.3 Add `Paragraph.bookmarkMarkers: [BookmarkRangeMarker]` to `packages/ooxml-swift/Sources/OOXMLSwift/Models/Paragraph.swift` (parallel array alongside existing `bookmarks`).
- [x] 2.4 Extend `DocxReader` paragraph child walker to parse `<w:bookmarkStart w:id="..." w:name="..."/>` (creating a `Bookmark` in `Paragraph.bookmarks` plus a `.start` `BookmarkRangeMarker` in `bookmarkMarkers`) and `<w:bookmarkEnd w:id="..."/>` (creating a `.end` marker). Implements spec requirement "DocxReader parses bookmark range markers as Paragraph children". Assign `position` from the in-walk counter. Verify Phase 2 tests pass.

## 3. Phase 3 — Wrapper hybrid model (Hyperlink, FieldSimple, AlternateContent)

- [x] 3.1 [P] Write failing test `testHyperlinkRunsAndRawAttributesRoundTrip` in `DocumentXmlLosslessRoundTripTests.swift` covering the spec requirement "Structural wrapper elements round-trip lossless across no-op save" for the hyperlink case (multi-run hyperlink with `r:id` + `w:tooltip`, one run bold, asserts Reader populates typed `runs` + `relationshipId` + `tooltip` and Writer emits matching XML).
- [x] 3.2 Modify `packages/ooxml-swift/Sources/OOXMLSwift/Models/Hyperlink.swift` to add fields `runs: [Run]`, `rawAttributes: [String: String]`, `rawChildren: [String]`, `position: Int` per design decision "Hybrid model (typed surface + raw passthrough), not raw-only carriers". Convert existing stored `text: String` to a computed property `runs.map { $0.text }.joined()` per design decision "`hyperlink.text` becomes computed property (option a from discuss)" (also written `Hyperlink.text` in the model — same field). Setter `text = "foo"` collapses to `runs = [Run(text: "foo")]`.
- [x] 3.3 Extend `DocxReader` to parse `<w:hyperlink>` direct children of `<w:p>`, populate `Paragraph.hyperlinks` with full typed Hyperlink instances per spec requirement "DocxReader parses hyperlink wrapper with typed runs and raw passthrough" (recursively parse inner `<w:r>` children using existing Run parser; capture `r:id`/`w:anchor`/`w:tooltip`/`w:history`/etc; raw passthrough for unrecognized attrs/children).
- [x] 3.4 [P] Write failing test `testFieldSimpleSEQTableCaptionRoundTrips` covering inserting a `<w:fldSimple w:instr=" SEQ Table \* ARABIC ">` block with one inner Run, asserting Reader populates `Paragraph.fieldSimples` with preserved `instr` whitespace and Writer emits matching XML at original position.
- [x] 3.5 [P] Create `packages/ooxml-swift/Sources/OOXMLSwift/Models/FieldSimple.swift` defining `public struct FieldSimple: Equatable { public var instr: String; public var runs: [Run]; public var rawAttributes: [String: String]; public var position: Int }` with appropriate initializers (typed editable surface per design decision "Hybrid model (typed surface + raw passthrough), not raw-only carriers").
- [x] 3.6 Add `Paragraph.fieldSimples: [FieldSimple]` field. Extend `DocxReader` to parse `<w:fldSimple>` direct children of `<w:p>` per spec requirement "DocxReader parses fldSimple wrapper as typed FieldSimple model" (preserve `w:instr` whitespace exactly; recursively parse inner runs; capture unrecognized attrs).
- [x] 3.7 [P] Write failing test `testAlternateContentMathBlockRoundTrips` covering an `<mc:AlternateContent>` block with `<mc:Choice>` (1 KB drawing) and `<mc:Fallback>` (text "Pearson (Spearman)"), asserting Reader populates `Paragraph.alternateContents` with byte-identical `rawXML` and `fallbackRuns` extracted from `<mc:Fallback>`, and Writer emits the `rawXML` byte-equivalent.
- [x] 3.8 [P] Create `packages/ooxml-swift/Sources/OOXMLSwift/Models/AlternateContent.swift` defining `public struct AlternateContent: Equatable { public var rawXML: String; public var fallbackRuns: [Run]; public var position: Int }` with initializer (third instance of design decision "Hybrid model (typed surface + raw passthrough), not raw-only carriers" — `rawXML` is the byte-preservation passthrough, `fallbackRuns` is the typed editable mirror).
- [x] 3.9 Add `Paragraph.alternateContents: [AlternateContent]` field. Extend `DocxReader` to parse `<mc:AlternateContent>` direct children per spec requirement "DocxReader parses AlternateContent wrapper preserving raw XML and extracting fallback runs" (capture verbatim XML; extract `<mc:Fallback>` runs into typed `fallbackRuns`).

## 4. Phase 4 — `<w:p>` child schema completeness (raw-carrier markers)

- [x] 4.1 [P] Write failing test `testCommentRangeMarkersRoundTrip` covering a paragraph with `<w:commentRangeStart w:id="3"/>` and `<w:commentRangeEnd w:id="3"/>` markers, asserting Reader populates `Paragraph.commentRangeMarkers` and Writer emits at original positions (per spec scenario in requirement "DocxReader parses unmodeled <w:p> children as raw-carrier markers with position").
- [x] 4.2 Extend `ParagraphChildMarkers.swift` with the remaining six raw-carrier types: `CommentRangeMarker`, `PermissionRangeMarker`, `ProofErrorMarker`, `SmartTagBlock`, `CustomXmlBlock`, `BidiOverrideBlock` — each with `rawXML: String` (or typed id field for range markers) plus `position: Int`. Add corresponding fields on `Paragraph`: `commentRangeMarkers`, `permissionRangeMarkers`, `proofErrorMarkers`, `smartTags`, `customXmlBlocks`, `bidiOverrides`.
- [x] 4.3 Add `Paragraph.unrecognizedChildren: [(name: String, xml: String, position: Int)]` fallback collection so unknown elements survive round-trip even without a dedicated raw-carrier (covers spec gaps surfaced by NTPU thesis sampling per design decision "ecma-376 `<w:p>` schema as the completeness checklist"; also written "ECMA-376" in design body — same decision).
- [x] 4.4 Extend `DocxReader` paragraph walker to dispatch each unmodeled `<w:p>` child element to its corresponding raw-carrier collection by local name (`commentRangeStart` → `commentRangeMarkers`, `commentRangeEnd` → same, `permStart` → `permissionRangeMarkers`, `permEnd` → same, `proofErr` → `proofErrorMarkers`, `smartTag` → `smartTags`, `customXml` → `customXmlBlocks`, `dir` → `bidiOverrides`, `bdo` → `bidiOverrides`). Unknown names go to `unrecognizedChildren` with a stderr warning.
- [x] 4.5 Refactor `Paragraph.toXML()` in `Paragraph.swift` to implement design decision "Position-index ordering, not enum refactor": collect every child instance from every parallel array (`runs`, `bookmarkMarkers`, `hyperlinks`, `fieldSimples`, `alternateContents`, plus all 6 marker collections, plus `unrecognizedChildren`), sort by `position` ascending, emit XML in sorted order. This is the implementation of spec requirement "WordDocument paragraph child elements have typed model or raw-carrier with position index".
- [x] 4.6 Add `Run.position: Int` and `Bookmark`-position propagation (the existing `Bookmark` struct on `Paragraph.bookmarks` does not need `position` because position lives on `BookmarkRangeMarker`; verify `Paragraph.toXML()` correctly drives bookmark emission from `bookmarkMarkers` not from `bookmarks`). Verify Phase 4 tests pass.

## 5. Phase 5 — Fixture + smoke test + release ceremony

- [x] 5.1 [P] Create `packages/ooxml-swift/Tests/OOXMLSwiftTests/Fixtures/LosslessRoundTripFixtureBuilder.swift` extending the existing `SDTFixtureBuilder` pattern. Synthesize a 50–100 KB `.docx` containing: 5+ bookmarks, 3+ hyperlinks (one URL with `r:id`, one anchor, one email `mailto:`), 2+ `<w:fldSimple>` blocks (one SEQ Table caption, one REF cross-reference), 1+ `<mc:AlternateContent>` math block with both `<mc:Choice>` and `<mc:Fallback>`, 10+ `xmlns:*` declarations on `<w:document>` root, mixed runs/wrappers across 3+ paragraphs to exercise position-index ordering. Implements design decision "Test fixture dual-track (synthesize + real-world smoke)".
- [x] 5.2 Add CI regression test `testBuilderFixtureRoundTripsLosslessOn34Namespaces` that uses `LosslessRoundTripFixtureBuilder` to generate a doc, runs `open → no-op save → reload`, and asserts all bookmark/hyperlink/fldSimple counts match plus `xmllint --noout` clean.
- [x] 5.3 [P] Create `mcp/che-word-mcp/Tests/CheWordMCPTests/RealWorldDocxRoundTripSmokeTests.swift` implementing the spec requirement "Real-world docx round-trip smoke test validates end-to-end lossless guarantee": iterate `mcp/che-word-mcp/test-files/*.docx`, for each file run `open → no-op save → reload` then assert (a) `xmllint --noout` clean, (b) bookmark count match, (c) `<w:hyperlink>` count match, (d) `<w:fldSimple>` count match, (e) `<mc:AlternateContent>` count match, (f) SHA256 of concatenated `<w:t>` content matches. `XCTSkip` when directory is absent or empty.
- [x] 5.4 Add `mcp/che-word-mcp/test-files/.gitkeep` plus update `mcp/che-word-mcp/.gitignore` to exclude `test-files/*.docx` (mirrors the existing `.note` smoke test pattern from PsychQuant/macdoc#81). Document fixture-population steps in `mcp/che-word-mcp/README.md` developer notes.
- [x] 5.5 Add scenario test `testReplaceTextInsideHyperlinkAppliesAndPersists` covering spec requirement "Tool-mediated edits inside structural wrappers SHALL apply (no silent failure)": load doc with hyperlink containing `[tab:foo]`, call `replace_text("[tab:foo]", "Table 1")`, save, reload, assert hyperlink runs reflect the change AND `w:anchor` attribute preserved. Add similar test for `format_text` inside `FieldSimple`.
- [x] 5.6 Run full test suite (`swift test` in ooxml-swift + `swift test` in che-word-mcp). Tag `ooxml-swift v0.19.0` and release. Bump `mcp/che-word-mcp/Package.swift` to `ooxml-swift from: "0.19.0"`. Run che-word-mcp test suite; should pass with zero source changes (additive). Tag `che-word-mcp v3.13.0`, release binary, update `mcp/che-word-mcp/CHANGELOG.md` and `mcp/che-word-mcp/mcpb/manifest.json`. Update `packages/ooxml-swift/CHANGELOG.md` v0.19.0 entry. Verify the design's Goals (byte-equivalent root, all 45 bookmarks preserved, tool-mediated edits inside wrappers apply, `xmllint --noout` clean, NTPU thesis survives `open → insert_paragraph → save`) all pass against the smoke fixture, and that the design's Non-Goals (Word's `mc:Choice` reconciliation, performance microbenchmarks, migrating 218 callers to `Hyperlink.runs`, hotfixing Phase 1 separately as v3.12.1, CustomXml part round-trip, `update_caption`/`update_all_fields` typing migration) are all out of scope for this release as documented.
