## 1. Phase A — Run-layer rawElements carrier (Decision: Run-layer-only fix for #52; defer Container/Paragraph layers, Decision: Generic `[(name: String, xml: String)]` carrier over typed `pict: VMLPicture?` model)

- [x] 1.1 In `packages/ooxml-swift/Tests/OOXMLSwiftTests/RunRawElementPreservationTests.swift`, write failing TDD tests covering all 3 spec scenarios from "Run preserves unknown OOXML child elements via rawElements carrier" requirement: (a) VML watermark Run round-trips byte-equal — parse `<w:r><w:rPr><w:noProof/></w:rPr><w:pict><v:shape>...</v:shape></w:pict></w:r>` and assert `Run.rawElements == [(name: "pict", xml: "<w:pict>...</w:pict>")]`, then `Run.toXML()` emits the verbatim `<w:pict>` block; (b) Multiple unknown elements preserve source order — parse Run with both `<w:pict>` and `<w:object>` and assert array order; (c) Equatable conformance — `Run(text: "hello") == parseRun("<w:r><w:t>hello</w:t></w:r>")` returns true. Run to confirm RED.
- [x] 1.2 In `packages/ooxml-swift/Sources/OOXMLSwift/Models/Run.swift`, add `public var rawElements: [(name: String, xml: String)]?` field with default `nil`. Add to public initializer with default `nil`. Verify Equatable conformance auto-derives correctly (tuple equality uses element-wise comparison).
- [x] 1.3 In `packages/ooxml-swift/Sources/OOXMLSwift/IO/DocxReader.swift::parseRun` (lines 850-878), add a final loop after existing typed-child handlers that iterates ALL children of the `<w:r>` element, skips recognized typed kinds (`rPr`, `t`, `drawing`, `oMath`, `oMathPara`), and appends each unknown element's `(localName, xmlString)` to `run.rawElements`. Set `rawElements = nil` (not empty array) when no unknowns found. Verify task 1.1 tests turn GREEN.
- [x] 1.4 In `packages/ooxml-swift/Sources/OOXMLSwift/Models/Run.swift` (or `Run+XML.swift` if XML emission is in a separate file), modify `Run.toXML()` to emit `rawElements` entries verbatim AFTER all typed children but BEFORE the closing `</w:r>` tag. When `rawElements` is `nil` or empty, emit no extra content (no-op). Verify task 1.1 tests turn GREEN.

## 2. Phase B — Header/Footer byte-equality integration test (Decision: New capability `ooxml-header-footer-raw-element-preservation`; MODIFY existing `che-word-mcp-field-equation-crud`)

- [x] 2.1 [P] In `packages/ooxml-swift/Tests/OOXMLSwiftTests/HeaderFooterByteEqualityWithVMLTests.swift`, write 2 failing TDD tests covering "Header/Footer round-trip preserves VML watermarks via Run-layer carrier" requirement scenarios: (a) `testUpdateAllFieldsPreservesVMLWatermarkInHeader` — build src doc with body Figure SEQ + header Run carrying injected `rawElements: [(name: "pict", xml: "<w:pict><v:shape string=\"DRAFT\"/></w:pict>")]`, write via DocxWriter, read back, run `updateAllFields`, save, re-read, assert `word/header1.xml` byte content for `<w:pict>` substring equals source; (b) `testUpdateHeaderPreservesVMLWatermark` — same fixture pattern but trigger via `WordDocument.updateHeader(id:text:)` instead. Run to confirm RED (Phase A's `parseRun` collection happens at read time; for tests built via programmatic `Run` construction with explicit `rawElements`, this exercises the writer-emit path independently).
- [x] 2.2 Verify Phase A integration: with Phase A complete, Phase B tests should pass without any Header/Footer-side code changes — `Header.toXML()` already calls `Paragraph.toXML()` which calls `Run.toXML()`. Document this dependency chain in a code comment on `Header.toXML()` referencing the new spec capability.

## 3. Phase C — updateAllFields isolatePerContainer flag (Decision: `update_all_fields(isolatePerContainer:)` opt-in flag, default `false`)

- [x] 3.1 In `packages/ooxml-swift/Tests/OOXMLSwiftTests/UpdateAllFieldsCounterIsolationTests.swift`, write failing TDD tests covering both new spec scenarios from MODIFIED `che-word-mcp-field-equation-crud` requirement: (a) `testDefaultBehaviorSharesCountersAcrossContainers` — body 3 Figure SEQ + header 1 Figure SEQ, call `updateAllFields()` (no flag), assert body Figures = 1/2/3 and header Figure = 4 (global sharing); (b) `testIsolationFlagResetsCountersPerContainerFamily` — same fixture, call `updateAllFields(isolatePerContainer: true)`, assert body Figures = 1/2/3 and header Figure = 1. Run to confirm RED.
- [x] 3.2 In `packages/ooxml-swift/Sources/OOXMLSwift/Models/WordDocument+UpdateAllFields.swift`, add `isolatePerContainer: Bool = false` parameter to `updateAllFields`. When `false`, use existing single `[String: Int] counters` dict. When `true`, allocate per-container-family counter dictionaries: one for body, one per `Header` instance (keyed by `header.id`), one per `Footer` instance, one for footnotes collection, one for endnotes collection. Each `processParagraph` call uses the counter dict matching its current container. Verify task 3.1 tests turn GREEN.
- [x] 3.3 In `mcp/che-word-mcp/Sources/CheWordMCP/Server.swift`, modify the existing "update_all_fields MCP tool recomputes SEQ counters across the document" tool registration: add `isolate_per_container` parameter (boolean, default false) to the tool input schema. In the handler, parse the parameter and pass through to `WordDocument.updateAllFields(isolatePerContainer:)`. Update tool description to document the parameter and reference v3.8.0 CHANGELOG migration section.

## 4. Phase D — ooxml-swift v0.14.0 release (Decision: Ship as `ooxml-swift v0.14.0` MINOR + `che-word-mcp v3.8.0` MINOR)

- [x] 4.1 Verify all `swift test` pass in ooxml-swift (expect 444 baseline + 7+ new = 451 minimum: 3 RunRawElement + 2 HeaderFooterByteEqualityWithVML + 2 UpdateAllFieldsCounterIsolation). Update `packages/ooxml-swift/CHANGELOG.md` with v0.14.0 entry citing closes che-word-mcp#52, the Run-layer raw-element carrier design, and the `isolatePerContainer` flag addition. Commit, push, `git tag v0.14.0`, `git push --tags`, `gh release create v0.14.0` with detailed notes.

## 5. Phase E — che-word-mcp v3.8.0 release + close (Decision: Spec impact split — modify `che-word-mcp-field-equation-crud` to remove v3.7.1 known-limitation paragraph)

- [x] 5.1 In `mcp/che-word-mcp/Package.swift`, bump ooxml-swift dep from `0.13.5` to `0.14.0`. Run `swift package update`. Verify `swift test` passes (expect 111 baseline + N from Phase C MCP tool tests).
- [x] 5.2 Update `mcp/che-word-mcp/mcpb/manifest.json` to v3.8.0 + `CHANGELOG.md` v3.8.0 entry citing closes #52, the new `isolate_per_container` MCP tool parameter, and explicit migration note removing the v3.7.1 "header WITH SEQ + VML still strips" known-limitation paragraph.
- [x] 5.3 Build universal binary (`swift build -c release --arch arm64 --arch x86_64`); copy to `~/bin/CheWordMCP` + `mcpb/server/CheWordMCP`; `xattr -cr` + `codesign --force --sign -`; repackage `mcpb/che-word-mcp.mcpb`.
- [x] 5.4 Commit + push + `git tag v3.8.0` + `git push --tags` + `gh release create v3.8.0` with detailed notes; `gh api`-based curl-upload binary + .mcpb assets (proven pattern from v3.7.x to avoid `gh release create --upload-asset` timeout). **Use `Refs #52` only — do NOT use `Closes #52` trailer per IDD git workflow rule (auto-close bypasses idd-close gate; v3.7.1 incident lesson saved to memory).**
- [x] 5.5 Bump `psychquant-claude-plugins/.claude-plugin/marketplace.json` and `plugins/che-word-mcp/.claude-plugin/plugin.json` to 3.8.0; commit + push; `claude plugin marketplace update psychquant-claude-plugins`; `claude plugin update che-word-mcp@psychquant-claude-plugins`.
- [x] 5.6 Run `/idd-verify #52` (default engine: 5 Claude reviewers + Codex if available, else self-review fallback) to verify byte-equality round-trip claim with real-fixture verification if NTPU thesis fixture is present in working tree.
- [x] 5.7 Run `/idd-close #52` with comprehensive closing summary citing the four Decisions, root cause (Run-layer drop at parseRun:850-878), bundle scope (raw-element preservation + counter-isolation), v3.7.1 known-limitation removal, and verification (ooxml-swift 451+ tests + che-word-mcp 111+ tests).
- [x] 5.8 Run `/spectra-archive che-word-mcp-header-footer-raw-element-preservation` to archive the SDD; verify spec sync applied ADDED requirements to `openspec/specs/ooxml-header-footer-raw-element-preservation/spec.md` (new) and MODIFIED `openspec/specs/che-word-mcp-field-equation-crud/spec.md` (replaces existing `update_all_fields` requirement with the expanded version including isolation + VML preservation scenarios).
