## 1. Phase 1 — Dependency chain unblocks

- [x] 1.1 [P] latex-math-swift: bump ooxml-swift requirement to from: 1.4.0 in packages/latex-math-swift/Package.swift, then in that repo run swift package clean + swift build + swift test; fix any 1.x breakage (compatibility unproven — no macdoc-side consumer ever built it against 1.4); commit, push, tag the next 0.x minor (read current from git tag), publish the GitHub release. → satisfies proposal What-Changes 1 · design Decisions 3, 7 + risk table row 1 (first task by design: if breakage is large, pause and revise estimate before touching che-word-mcp). Verify: swift test green in latex-math-swift against ooxml 1.4; release tag visible on GitHub.
- [x] 1.2 [P] word-to-md-swift: bump ooxml-swift requirement to from: 1.4.0 in packages/word-to-md-swift/Package.swift; swift package clean + build + test in that repo (source already proven 1.4-compatible by macdoc's graph — expect zero code changes); commit, push, tag next 0.x minor, publish release. Then in macdoc root run swift package clean + swift build to confirm the path-dep graph still resolves. → satisfies What-Changes 1 · Decisions 3, 7. Verify: word-to-md-swift suite green; macdoc root build green.

## 2. Phase 2 — che-word-mcp cross-major migration

- [x] 2.1 In mcp/che-word-mcp/Package.swift bump ooxml-swift to from: 1.4.0, word-to-md-swift and latex-math-swift to their new minors from Phase 1; swift package clean (payload struct layouts changed across 0.x→1.x); build and fix HARD breaking only — known: RunProperties.rawChildren removal, tree-only IO path (v1.0.0 CHANGELOG); leave direct-typed-mutation deprecation warnings in place (Decision 4 — no op-based rewrites); run the full existing test suite to green. → satisfies What-Changes 2 · Decision 4. Verify: swift test green in mcp/che-word-mcp on ooxml 1.4.0; zero tool contract changes (grep confirms no existing tool name/schema edits).

## 3. Phase 3 — Parity tools (TDD: test first per task)

- [x] 3.1 Layer-1 parity test (RED first): create mcp/che-word-mcp/Tests/CheWordMCPTests/ScriptPipelineParityTests.swift with an ungated in-process test — build a synthetic five-layer docx via typed ops (pattern: ooxml-swift ContentSlotTests.makeReference), call the not-yet-existing handler functions export → execute, assert Stage-B byte equality of rebuilt XML part set; create mcp/che-word-mcp/Sources/CheWordMCP/ScriptPipelineTools.swift with handler function signatures wrapping ReverseExtractor/ScriptExporter/ScriptImporter (Decision 1: thin wrappers, zero reimplemented logic) until the test compiles and passes. → satisfies «Two-layer parity guard» (ungated scenario) · Decisions 1, 5, 6. Verify: plain swift test runs the parity test (not skipped) and it passes.
- [x] 3.2 export_script tool: register in Server.swift, handler in ScriptPipelineTools.swift; params source_path, output_path, slots?: [{name, para_id}] (paragraphs_only dropped per amended Decision 2 — macdoc-side legacy logic, not shared-library); response summary {dsl_parts, form_gaps_empty, slot_count}, script text never inlined (design risk row 4); strict-mode slot failures map to MCP errors carrying TranscodeError description — test first from spec scenario "Strict slot failure surfaces as a tool error" (unknown para_id → error, no file written). → satisfies «Script export rides the CLI's code path» · Decisions 1, 2. Verify: scenario test green; tool appears in list_tools output.
- [x] 3.3 get_script_coverage tool: register + handler; response {parts: [{part_path, channel, bytes, dsl_ratio}], aggregate_ratio} from the same coverage APIs as the CLI report; test first with a synthetic fixture asserting row shape and known ratios, plus an env-gated JPA-template test from spec scenario "JPA template coverage parity" (document.xml channel dsl, ratio 1.0). → satisfies «Coverage report matches the CLI numbers» · Decisions 1, 2. Verify: ungated shape test green; gated test green on maintainer machine, loud skip without MACDOC_TEMPLATE_DIR.
- [x] 3.4 execute_script tool: register + handler; params script_path, output_path, verify_byte_equal_against?: path; PartFidelity verdict with broken-part list on mismatch; parse failures map to MCP errors with location-bearing reason — tests first from spec scenarios "Rebuild verifies byte-equal against the reference" and "Slot substitution through the MCP surface" (substituted text lands, non-slot parts stay byte-equal). → satisfies «Script execution rebuilds with verifiable byte equality» · Decisions 1, 2. Verify: both scenario tests green.
- [x] 3.5 Layer-2 gated cross-check test: when MACDOC_TEMPLATE_DIR and MACDOC_CLI_PATH are both set, run the JPA template through MCP handlers and through the macdoc CLI (word reverse + script execution), assert exported scripts and rebuilt part sets byte-identical; either gate absent → XCTSkip naming the missing gate (spec scenario "Gated cross-check skips loudly"). → satisfies «Two-layer parity guard» (gated scenario) · Decision 6. Verify: dual-gated run green on maintainer machine; single-gate runs skip naming the gate.

## 4. Phase 4 — Release chain and marketplace sync

- [x] 4.1 che-word-mcp release: bump version, CHANGELOG entry (migration + three tools + parity guard), run make release-signed (Developer ID F2523DCF…, keychain profile che-mcps-notary, per the che-mcps pipeline), publish the GitHub release with binary + sha256 + mcpb assets. → satisfies What-Changes 5 · Decision 7. Verify: notarization accepted; release assets present.
- [x] 4.2 macdoc marketplace sync per #116 (this IS a binary release → binary_version moves): bump plugins/che-word-mcp/.claude-plugin/plugin.json version + binary_version and .claude-plugin/marketplace.json; run the plugin-update flow per common-release-flow.md post-release chain; update macdoc CLAUDE.md's che-word-mcp tool-count line (145 → new count) and note the script-pipeline tools. → satisfies What-Changes 5 · Decision 7 + design acceptance (e). Verify: claude plugin update pulls the new binary_version; CLAUDE.md consistent with shipped tool count.
