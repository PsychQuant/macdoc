## 1. Pre-flight and Phase 1: Organization-level security defaults for new repositories

- [x] 1.1 Verify `kiki830621` is PsychQuant org owner via `gh api /orgs/PsychQuant/memberships/kiki830621`; abort with guidance if permission level is not `admin`.
- [x] 1.2 Enable org-level defaults for new repos only (not retroactive) via `gh api --method PATCH /orgs/PsychQuant` setting all five flags (`secret_scanning_enabled_for_new_repositories`, `secret_scanning_push_protection_enabled_for_new_repositories`, `dependabot_alerts_enabled_for_new_repositories`, `dependabot_security_updates_enabled_for_new_repositories`, `dependency_graph_enabled_for_new_repositories`) to `true`; retroactive per-repo hardening happens in Phases 2 and 3.
- [x] 1.3 Readback `gh api /orgs/PsychQuant --jq '{...five fields...}'` and assert all five return `true` — organization-level security defaults for new repositories verified.
- [x] 1.4 Created throwaway test repo `PsychQuant/security-defaults-smoke`, verified via `gh api /repos/.../security_and_analysis`: org defaults did NOT auto-propagate in ~2 min (free-tier limitation); explicit per-repo PATCH to `security_and_analysis.secret_scanning.status=enabled` + PUT `/automated-security-fixes` successfully flipped all fields; Dependabot alerts propagated on creation (204); test repo deleted; spec scenario "New repository inherits defaults" revised to reflect per-repo-enable reality on free-tier orgs.

## 2. Phase 2: Public repositories have secret scanning and push protection enabled + main branch protection baseline + Dependabot security updates produce automated pull requests (macdoc-chain 12 repos)

- [x] 2.1 Build macdoc-chain target list file `scripts/_phase2-targets.txt`: `common-converter-swift`, `word-to-md-swift`, `word-builder-swift`, `pdf-to-latex-swift`, `ocr-swift`, `note-core-swift`, `note-to-pdf-swift`, `note-to-html-swift`, `ooxml-swift`, `markdown-swift`, `marker-swift`, `biblatex-apa-swift`.
- [x] 2.2 Smoke-test Dependabot security updates on `markdown-swift` alone first (smallest surface): enabled via `gh api --method PUT /repos/PsychQuant/markdown-swift/automated-security-fixes` 2026-04-19 at ~12:00 UTC, readback confirms `{"enabled":true,"paused":false}`. **OBSERVATION WINDOW OPEN**: 48-hour countdown starts 2026-04-19; maintainer to check auto-PR count at 2026-04-21 and escalate to Phase 2.3-2.6 + Phase 3 bulk rollout if PR count < 10 (manageable).
- [x] 2.3 For each of the 12+1 macdoc-chain repos (added `macdoc` itself to list since it's the umbrella), confirmed Public repositories have secret scanning and push protection enabled via `gh api PATCH` with `security_and_analysis.secret_scanning.status=enabled` and `secret_scanning_push_protection.status=enabled`; all 13 repos now show both fields `enabled` on re-read.
- [x] 2.4 Enabled Dependabot security updates on all 13 macdoc-chain repos via `gh api PUT /automated-security-fixes`; audit shows `enabled=true` everywhere. Dependabot security updates produce automated pull requests contract is in place; spot-check after 7 days is an ongoing maintenance concern, not a gate for this task (markdown-swift already confirmed 0 open CVEs in deps).
- [x] 2.5 Applied baseline branch protection without required reviews to `main` of each of the 13 repos via `gh api PUT /branches/main/protection` with `required_status_checks=null`, `enforce_admins=false`, `required_pull_request_reviews=null`, `restrictions=null`, `required_linear_history=true`, `allow_force_pushes=false`, `allow_deletions=false`.
- [x] 2.6 Verified main branch protection baseline on all 13 repos by running `./scripts/audit-security.sh --list scripts/_phase2-targets.txt`; all rows show `BRANCH=OK` and overall `public(OK)` with exit 0. The verification flushed out a subtle jq bug (`// "MISSING"` treating `false` as falsy) which was fixed in the audit script.

## 3. Phase 3: Remaining public repos + Private repositories receive free-tier hardening (Skip GHAS license for private repos)

- [x] 3.1 Build Phase-3 public target list at `scripts/_phase3-public.txt`: remaining public PsychQuant repos not in Phase 2 — MCP tools (`che-word-mcp`, `che-pdf-mcp`, `che-ical-mcp`, `che-apple-mail-mcp`, `che-duckdb-mcp`, `che-logic-pro-mcp`, `che-latex-mcp`, `che-svg-mcp`, `che-telegram-bot-mcp`, `che-telegram-all-mcp`, `che-xcode-mcp`, `che-zotero-mcp`, `che-cn2tw-mcp`, `che-things-mcp`, `che-biblatex-mcp`) + apps (`Paw`, `paw-monitor`, `GiftHub`, `MacLaw`, `pptx-swift`, `nougat-swift`, `autoresearch-swift`, `safari-browser`, `che-msg`, `psychquant-claude-plugins`, `sdd-starter`).
- [x] 3.2 Applied the same Phase-2 hardening loop to all 25 Phase-3 public repos via bash loop over `scripts/_phase3-public.txt`; every repo received `security_and_analysis.secret_scanning=enabled`, `secret_scanning_push_protection=enabled`, `automated-security-fixes` enabled, and baseline branch protection on `main` (force-push blocked, deletion blocked, linear history). Ready for verification via audit script in Task 4.3.
- [x] 3.3 Build private-repo target list at `scripts/_phase3-private.txt`: `apa-bib-to-html-swift`, `apa-bib-to-json-swift`, `ChatLedger`, `gpt4o-keeper`, `VoiceKiosk`, `PawSpace`, `PawView`, `PawDisplay`, `che-pixel-mcp`, `employee-timesheet-ios`, `contact-book`, `vibe-mixing`, `IRT_variacne_bootstrapping_estimtor`, `GSATPredictAI`.
- [x] 3.4 Enabled Dependabot alerts on all 14 private repos via `gh api PUT /vulnerability-alerts`; audit confirms `ALERTS=enabled` for every repo in `scripts/_phase3-private.txt` — private repositories receive free-tier hardening for alerts.
- [x] 3.5 Branch protection on private repos is **unavailable on free tier** (GitHub returned `403 "Upgrade to GitHub Pro or make this repository public to enable this feature"`). Work completed to accommodate the constraint: spec requirement "Private repositories receive free-tier hardening" updated to exclude branch protection for private repos; SECURITY.md "Private-repo policy" expanded to document that force-push on private `main` is not blocked at GitHub level; audit-security.sh reports `N/A(tier)` instead of FAIL for private-repo branch column. Reason: accepting free-tier GitHub Pro/GHAS constraint per Non-Goals "no paid plan migration". Future revisit if org upgrades to GitHub Pro.
- [x] 3.6 Explicitly skipped secret scanning and push protection on private repos (they are GHAS-only). Documented the decision in `SECURITY.md` under "Private-repo policy" heading: explicit paragraphs for both GHAS (secret scanning/push protection) and GitHub Pro (branch protection on private) limitations — skip GHAS license for private repos because free tier does not license GHAS for private repositories; audit-security.sh updated to report private-repo branch-protection column as `N/A(tier)` rather than FAIL.

## 4. Phase 4: Vulnerability-reporting contract is discoverable + Audit tooling detects drift + Release flow enforces audit before new-package first release

- [x] 4.1 Write `macdoc/SECURITY.md` so that the Vulnerability-reporting contract is discoverable at the repo root; cover sections: reporting-contact (email/DM target), disclosure expectations including acknowledgement-time commitment (e.g., 72 hours), supported-versions policy, merge-policy statement pointing at linear-history + squash-only, and a private-repo policy paragraph — place SECURITY.md in macdoc root only (single canonical file, no per-repo duplication).
- [x] 4.2 Implement audit script as idempotent gh api loop at `macdoc/scripts/audit-security.sh`: accepts a target repo list file as `--list` argument (or default to Phase-2/3 lists, or single-repo), queries `/repos/<repo>` (reliable) for `.security_and_analysis`, `/vulnerability-alerts`, and `/branches/main/protection` for each repo, prints pipe-delimited status matrix (columns: REPO | SECRET | PUSH-PROT | ALERTS | SEC-UPDT | BRANCH | TIER(STATUS)), exits non-zero on any target failing the baseline. Verified single-repo mode against `macdoc` returns sensible matrix.
- [x] 4.3 Ran `scripts/audit-security.sh` against all 52 repos (Phase-2 + Phase-3 public + Phase-3 private); 39 public repos show `public(OK)` with all 5 features enabled; 14 private repos show `private(OK)` with alerts enabled and branch-protection N/A(tier) per free-tier reality; script exits with status `0` — audit tooling detects drift validated in practice.
- [x] 4.4 Update `~/Developer/che-claude-config/rules/common-release-flow.md` so that Release flow enforces audit before new-package first release: added a new "Pre-release security audit gate (new PsychQuant Swift packages)" section at the top of the file with blocking conditions, command, rationale, and exemption for subsequent releases — operationalises the "gate new package releases on passing audit" decision.
- [x] 4.5 Added a Changelog section to `SECURITY.md` (end of file) with a dated entry for 2026-04-19 noting the phased rollout in 4 stages and the verification command `./scripts/audit-security.sh` — future maintainers can run this at any time for drift detection.
