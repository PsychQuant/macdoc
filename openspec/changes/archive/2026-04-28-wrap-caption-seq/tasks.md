# Tasks

Phase 1 lands in `PsychQuant/ooxml-swift` (lib). Phase 2 lands in `PsychQuant/che-word-mcp` (MCP wrapper) AFTER Phase 1's v0.21.0 release tag is published, because the MCP wrapper requires the new `Document.wrapCaptionSequenceFields` API.

## Phase 1 — ooxml-swift lib (v0.21.0 minor — adds public API)

- [x] [P] 1.1 (design Decision 3 — scope vocabulary: "body" | "all" mirroring updateAllFields (A3); existing infrastructure (informs decisions) — TextScope shared enum) Add `Models/TextScope.swift` — `public enum TextScope: Equatable, Sendable { case body, all }`. Skip if a matching type already exists in the module (verify via `grep -r "enum TextScope" Sources/`).
- [x] [P] 1.2 (design Decision 5 — return shape: structured per-paragraph result (A5)) Add `Models/WrapCaptionResult.swift` — `public struct WrapCaptionResult: Equatable, Sendable` (matchedParagraphs Int, fieldsInserted Int, paragraphsModified [Int], skipped [SkippedParagraph]) + `public struct SkippedParagraph: Equatable, Sendable` (paragraphIndex Int, reason String, container String?).
- [x] [P] 1.3 (design Decision 1 — match interface: regex with one numeric capture group (A1); design Decision 2 — bookmark wrapping defaults off; opt-in via insert_bookmark + bookmark_template (A2)) Add `Models/WrapCaptionError.swift` — `public enum WrapCaptionError: Error, Equatable` cases `patternMissingCaptureGroup(actual: Int)`, `bookmarkTemplateMissing`, `scopeNotImplemented(TextScope)`.
- [x] 1.4 (design Decision 6 — lib placement: Document.wrapCaptionSequenceFields, MCP layer is thin (A6); design Decision 4 — format flag, replacement (not preservation), of user-typed numerals (A4); spec requirement Document.wrapCaptionSequenceFields converts plain-text caption number portions to SEQ-field runs) Add `Models/WordDocument+WrapCaptionSequenceFields.swift` with the public mutating method per design Decision 6, signature exactly per spec ooxml-content-insertion-primitives. Phase 1 implements `scope: .body` only; `scope: .all` throws `WrapCaptionError.scopeNotImplemented(.all)`.
- [x] 1.5 Implement traversal: walk `body.children`; for each `.paragraph` apply match; for `.table` recurse into `rows[].cells[].paragraphs` plus `cell.nestedTables`; for `.contentControl(_, children:)` recurse on children. Mirror surface-walking pattern from `Document.replaceInParagraphSurfaces` at `Sources/OOXMLSwift/Models/Document.swift` lines 326+.
- [x] 1.6 Implement run-splitting at the captured numeric range: locate the run whose accumulated text-offset window contains the capture start; split that run into [pre-text Run, SEQ-field Run with `rawXML = SequenceField(identifier: sequenceName, format: format, cachedResult: capturedString).toFieldXML()`, post-text Run]; rebuild the paragraph runs in document order. Empty pre-text or post-text runs SHALL NOT be inserted.
- [x] 1.7 Implement bookmark wrap branch: when `insertBookmark == true`, generate a unique bookmark id via `body.nextAvailableBookmarkId()` (use existing helper if present; otherwise add `internal func nextAvailableBookmarkId() -> Int` that scans existing bookmarks); insert `<w:bookmarkStart w:name="<template-substituted>" w:id="<id>">` immediately before the SEQ-field run and `<w:bookmarkEnd w:id="<same-id>">` after it; substitute literal `${number}` in `bookmarkTemplate` with the captured numeric.
- [x] 1.8 Implement idempotency check `paragraphAlreadyWrapsSEQ(paragraph: Paragraph, sequenceName: String) -> Bool`: scans `paragraph.fieldSimples` for `instr` containing `"SEQ \(sequenceName)"` AND scans `paragraph.runs[].rawXML` for `<w:instrText>SEQ \(sequenceName)` substring (covers both `insertCaption` rawXML emission and `FieldSimple` typed emission). Returns true if either match found.
- [x] 1.9 Add tests `Tests/OOXMLSwiftTests/WrapCaptionSequenceFieldsTests.swift` (TDD — RED first, GREEN after each implementation step):
  - 1.9.1 `testBodyScopeWrapsThreePlainTextFigureCaptions` (mirrors spec Scenario 1 — 3 fig captions matched + fields inserted + paragraphsModified ordering)
  - 1.9.2 `testIdempotentSecondCallSkipsAlreadyWrapped` (Scenario 2 — re-run reports all 3 in skipped)
  - 1.9.3 `testPatternWithZeroCaptureGroupsThrows` (Scenario 3)
  - 1.9.4 `testPatternWithTwoCaptureGroupsThrows` (Scenario 3 variant — actual count = 2)
  - 1.9.5 `testBookmarkWrappingWithTemplateSubstitution` (Scenario 4 — bookmark `fig7` from `Figure 7.`)
  - 1.9.6 `testInsertBookmarkTrueWithoutTemplateThrows` (Scenario 5)
  - 1.9.7 `testIdempotencyCoversBothFldSimpleAndRawXMLEmissions` (Scenario "skip detection covers both" — 1 fldSimple paragraph + 1 rawXML paragraph both reported skipped)
  - 1.9.8 `testCachedResultPreservesUserNumeralsBeforeF9` (after wrap, paragraph.flattenedDisplayText() still displays the original digits)
  - 1.9.9 `testTableCellAnchorParagraphWraps` (regression pin — table cell paragraph matched + wrapped)
  - 1.9.10 `testScopeAllThrowsScopeNotImplementedInPhase1` (lock the Phase 1 deferral so Phase 2 has clear contract)
- [x] 1.10 Add CHANGELOG entry under `## [0.21.0] - <date>` titled `### Added — wrapCaptionSequenceFields lib API (Refs PsychQuant/che-word-mcp#62)` covering: new public method, Phase 1 scope-body-only constraint, idempotency contract, bookmark wrap behavior. Note Phase 2 cross-container path lands in v0.21.1 alongside MCP wrapper integration test.
- [x] 1.11 Open PR `PsychQuant/ooxml-swift` titled `feat: wrap_caption_seq lib API (Refs PsychQuant/che-word-mcp#62)` with body referencing this Spectra change and the spec scenarios. STOP after PR opens — wait for human review + merge + tag v0.21.0 + GH release.

## Phase 2 — che-word-mcp MCP wrapper (v3.17.0 minor — adds tool, AFTER Phase 1 release)

- [x] 2.1 Bump `mcp/che-word-mcp/Package.swift` ooxml-swift dependency from `0.20.5` (or whatever lands first) to `0.21.0`. Run `swift package update` and confirm `swift build` succeeds.
- [x] 2.2 Add `wrap_caption_seq` tool schema entry in `Sources/CheWordMCP/Server.swift` near the `insert_caption` schema (line ~4815) — full schema with `pattern` / `sequence_name` / `format` (enum constraint) / `scope` (enum constraint) / `insert_bookmark` / `bookmark_template` properties; `description` covers the v3.17.0+ semantics including idempotency, regex contract (one numeric capture group), and `format` / `scope` accepted values.
- [x] 2.3 Add dispatcher case `case "wrap_caption_seq": return try await wrapCaptionSeq(args: args)` in the existing `dispatch(...)` switch (next to the other caption-related cases).
- [x] 2.4 Implement private `wrapCaptionSeq(args:)` handler in `Server.swift`: decode args; validate doc_id opened; compile `NSRegularExpression(pattern: ...)` returning `"Error: wrap_caption_seq: pattern failed to compile: <NSError reason>"` on failure; verify `regex.numberOfCaptureGroups == 1` returning explicit error; map format string → `SequenceField.Format` returning explicit error on unknown; map scope string → `TextScope` returning explicit error on unknown; validate `insert_bookmark`/`bookmark_template` interaction returning explicit error; call `doc.wrapCaptionSequenceFields(...)`; marshal `WrapCaptionResult` to JSON string with snake_case keys; persist via `storeDocument`; return success JSON. All error returns use `"Error: wrap_caption_seq: ..."` prefix per #70 convention.
- [x] 2.5 Add tests `Tests/CheWordMCPTests/Issue62WrapCaptionSeqTests.swift`:
  - 2.5.1 `testWrapCaptionSeqEndToEndOnThreeFigureCaptions` (12 figure captions in body — matches lib Scenario 1 via MCP dispatcher)
  - 2.5.2 `testWrapCaptionSeqIdempotentReRun` (verify second call produces all-skipped JSON)
  - 2.5.3 `testWrapCaptionSeqRejectsZeroCaptureGroupPattern` (verify error string format `"Error: wrap_caption_seq: pattern must contain exactly one capture group, got 0"`)
  - 2.5.4 `testWrapCaptionSeqRejectsBookmarkTrueWithoutTemplate`
  - 2.5.5 `testWrapCaptionSeqAfterCallEnablesUpdateAllFieldsAndTOF` (end-to-end value test: wrap_caption_seq → update_all_fields → insert_table_of_figures → verify TOF body now contains caption entries)
- [x] 2.6 Add CHANGELOG entry under `## [3.17.0] - <date>` titled `### Added — wrap_caption_seq MCP tool (Refs #62)` covering tool surface, idempotency, regex contract, format/scope accepted values, return shape JSON, `insert_table_of_figures` / `insert_table_of_tables` unblocked downstream. Bump `mcp/che-word-mcp/mcpb/manifest.json` version 3.16.x → 3.17.0 with description prefix.
- [x] 2.7 Run full test suite (`swift test`) and confirm 0 fail. Build release binary, repackage mcpb, verify SHA256 matches across `.build/release` / `~/bin` / `mcpb/server`.
- [x] 2.8 Tag `v3.17.0`, push tag, create GH release with mcpb asset.
- [x] 2.9 Marketplace sync per `che-claude-config/rules/common-release-flow.md`: bump `psychquant-claude-plugins/.claude-plugin/marketplace.json` and `plugins/che-word-mcp/.claude-plugin/plugin.json` to 3.17.0; commit + push; verify `claude plugin update che-word-mcp@psychquant-claude-plugins` reports `<old> → 3.17.0`.
- [x] 2.10 Open PR `PsychQuant/che-word-mcp` titled `feat: wrap_caption_seq MCP tool (Refs #62)` with body linking the lib PR, this Spectra change, the spec scenarios, and the v3.17.0 release URL. STOP after PR opens — wait for human review + merge + `/idd-close #62`.
