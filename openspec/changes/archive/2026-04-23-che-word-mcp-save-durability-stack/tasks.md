## 1. Phase 1 — ooxml-swift atomic save (closes PsychQuant/che-word-mcp#36)

- [x] 1.1 In `packages/ooxml-swift/Tests/OOXMLSwiftTests/AtomicSaveTests.swift`, write failing TDD tests for the "DocxWriter.write performs atomic-rename save" requirement: cover the 4 scenarios from the spec (successful save replaces target atomically, throw mid-write preserves original, process killed mid-write preserves original, fresh write to non-existent path). Tests assert temp-file naming pattern `<url>.tmp.<UUID>` and verify orphan cleanup via `defer`. Run `swift test --filter AtomicSaveTests` to confirm RED.
- [x] 1.2 In `packages/ooxml-swift/Sources/OOXMLSwift/IO/DocxWriter.swift`, implement Decision: Atomic save goes in ooxml-swift DocxWriter — rewrite `write(_:to:)` to atomic-rename pattern — write zip bytes to `url.appendingPathExtension("tmp.\(UUID().uuidString)")`, call `FileHandle.synchronize()` for fsync, then `FileManager.default.replaceItemAt(url, withItemAt: tempURL, backupItemName: nil, options: [], resultingItemURL: nil)`. Remove pre-emptive `removeItem(at: url)` (current line 19-21). Add `defer { try? FileManager.default.removeItem(at: tempURL) }` to handle throw paths. Verify task 1.1 tests turn GREEN. Also satisfies "DocxWriter.write supports cross-volume rename via Foundation fallback" requirement (replaceItemAt internally handles cross-volume).
- [x] 1.3 Verify all `swift test` pass in ooxml-swift (expect 391 + 4 new = 395 minimum). Update `packages/ooxml-swift/CHANGELOG.md` with v0.13.2 entry citing closes che-word-mcp#36 + atomic-rename architecture. Commit with `Refs PsychQuant/che-word-mcp#36` (NOT `Closes`), push, `git tag v0.13.2`, `git push --tags`, `gh release create v0.13.2` with detailed notes.
- [x] 1.4 In `mcp/che-word-mcp/Package.swift`, bump `ooxml-swift` dep to `from: "0.13.2"`; `swift package update`; `swift test` to verify all 79 che-word-mcp tests pass against new ooxml-swift; update `mcp/che-word-mcp/mcpb/manifest.json` to v3.5.3 + `CHANGELOG.md` entry; build universal binary (`swift build -c release --arch arm64` + `--arch x86_64` + `lipo -create` + `xattr -cr` + `codesign --force --sign -`); repackage `mcpb/che-word-mcp.mcpb`; commit + push + `git tag v3.5.3` + `gh release create v3.5.3` + curl-upload binary + .mcpb; bump `psychquant-claude-plugins` plugin.json + marketplace.json to 3.5.3; commit + push + `claude plugin marketplace update` + `claude plugin update`; run `/idd-close #36`.

## 2. Phase 2 — actor refactor (closes PsychQuant/che-word-mcp#39)

- [x] 2.1 In `mcp/che-word-mcp/Sources/CheWordMCP/Server.swift`, implement Decision: class → actor refactor for concurrency safety — change `class WordMCPServer` declaration to `actor WordMCPServer`. Run `swift build` to surface all isolation violations. Most existing handlers are already `async throws` so signature changes minimal. For any non-async method called by the MCP runtime that needs to access actor state, refactor to `async`. Verify the swift-sdk MCP framework's request dispatcher tolerates actor-isolated server (handlers are already `async throws` so should work; if not, document the blocker and pause for design revision). Satisfies "WordMCPServer is an actor for concurrent-safe session state" requirement.
- [x] 2.2 Audit every `await self.<method>()` path within mutating handlers for actor reentrancy invariant preservation. For each such path, verify mid-mutation state observed by reentering tasks is consistent (e.g., `openDocuments[docId]` fully assigned before any `await` that releases the actor). Document audit findings in commit message; refactor any path that breaks the invariant by reordering state writes to before the `await`. Satisfies "Actor reentrancy preserves mutation invariants" scenario.
- [x] 2.3 In `mcp/che-word-mcp/Tests/CheWordMCPTests/ActorIsolationStressTests.swift`, write `testParallelInsertImageDoesNotCrash`: spawn 50 concurrent `insert_image_from_path` tasks against the same `doc_id` via `await withTaskGroup(of: Void.self)`; run 100 iterations; enable ThreadSanitizer via `swift test -Xswiftc -sanitize=thread`. Assert: no iteration crashes, no TSan race report appears in output, `openDocuments[docId].images.count == 50` after all tasks complete in each iteration. Satisfies "50 concurrent mutations do not crash or corrupt state" scenario.
- [x] 2.4 Update `mcp/che-word-mcp/mcpb/manifest.json` to v3.5.4 + `CHANGELOG.md` entry citing closes #39 + actor refactor; rebuild universal binary; repackage `.mcpb`; commit + push + tag + gh release create with curl-upload; bump psychquant-claude-plugins to 3.5.4; commit + push + plugin marketplace sync + plugin update; run `/idd-close #39`.

## 3. Phase 3 — .bak preservation (closes PsychQuant/che-word-mcp#38)

- [x] 3.1 In `mcp/che-word-mcp/Tests/CheWordMCPTests/BakPreservationTests.swift`, write failing tests for the 4 scenarios from "save_document supports keep_bak opt-in for rollback escape hatch" requirement: (a) `keep_bak: true` preserves pre-save bytes (assert `<target>.bak` SHA256 matches original); (b) `keep_bak` default opt-out (no `.bak` created); (c) consecutive saves overwrite `.bak` (second save's `.bak` matches first save's output, NOT original); (d) first-time save with non-existent target produces no `.bak`. Run to confirm RED.
- [x] 3.2 In `mcp/che-word-mcp/Sources/CheWordMCP/Server.swift`, modify `persistDocumentToDisk` to implement Decision: .bak preservation at che-word-mcp server layer, opt-in — before calling `DocxWriter.write(doc, to: url)`, when `keepBak` flag is true AND target file exists, call `try FileManager.default.moveItem(at: url, to: url.appendingPathExtension("bak"))` (overwriting prior `.bak`). If target doesn't exist (first save), skip rename — no-op.
- [x] 3.3 In Server.swift, modify the `save_document` MCP tool registration to declare `keep_bak: Bool` parameter (default `false`) in the input schema. Thread `keep_bak` from the args dict through `saveDocument(args:)` handler down to `persistDocumentToDisk` via a new parameter. Verify task 3.1 tests turn GREEN.
- [x] 3.4 Update `mcp/che-word-mcp/mcpb/manifest.json` to v3.5.5 + `CHANGELOG.md` entry citing closes #38 + `.bak` opt-in; rebuild universal binary; repackage `.mcpb`; commit + push + tag + gh release with curl-upload; bump psychquant-claude-plugins to 3.5.5; commit + push + plugin marketplace sync + plugin update; run `/idd-close #38`.

## 4. Phase 4 — autosave + checkpoint + recover (closes PsychQuant/che-word-mcp#37)

- [x] 4.1 In `mcp/che-word-mcp/Tests/CheWordMCPTests/AutosaveCheckpointTests.swift`, write failing tests for the 9 scenarios across all 5 new Phase 4 requirements. Cover: autosave fires at every Nth mutation (with example N=3, 7 mutations expecting checkpoints at mutations 3 and 6); `autosave_every: 0` disables autosave entirely; successful `save_document` cleans up `<target>.autosave.docx`; manual `checkpoint` writes to autosave path by default; manual `checkpoint` with explicit path uses that path; `open_document` flags stale autosave via `autosave_detected: true`; `open_document` reports `autosave_detected: false` when no autosave file; `recover_from_autosave` replaces session state with autosave bytes and sets `is_dirty: true`; `recover_from_autosave` refused on dirty session without `discard_changes: true`. Run to confirm RED.
- [x] 4.2 In `Sources/CheWordMCP/Server.swift`, implement Decision: Autosave throttle = per-N-mutations with explicit recover tool — autosave_every part: add `autosaveEvery: [String: Int]` and `autosaveCounter: [String: Int]` actor-isolated dictionaries; add `autosave_every: Int = 0` parameter to `open_document` MCP tool registration and pass through to `initializeSession`; in `storeDocument` (line 199), when `autosaveEvery[docId] > 0`, increment counter and dispatch checkpoint write to `<sourcePath>.autosave.docx` via `DocxWriter.write` when `counter % autosaveEvery[docId] == 0`. Satisfies "open_document supports autosave_every for periodic checkpoint" requirement.
- [x] 4.3 In Server.swift, register new MCP tool `checkpoint` with input schema `{doc_id: String, path: String?}`. Implementation: resolve target path = `path` arg OR `<documentOriginalPaths[docId]>.autosave.docx`; call `DocxWriter.write(doc, to: target)`; do NOT call `storeDocument` (so `is_dirty` stays unchanged); do NOT update `documentDiskHash` / `documentDiskMtime`. Satisfies "checkpoint MCP tool for manual session state write" requirement.
- [x] 4.4 In Server.swift, register new MCP tool `recover_from_autosave` with input schema `{doc_id: String, discard_changes: Bool = false}`. Implementation: resolve `<source>.autosave.docx` path; if not found return `E_NO_AUTOSAVE`; if `documentDirtyState[docId] == true` and `discard_changes != true` return `E_DIRTY_DOC` with the same recovery options text as `close_document` dirty-check; otherwise call `DocxReader.read(from: autosaveURL)`, replace `openDocuments[docId]` with loaded document, set `documentDirtyState[docId] = true`, do NOT delete autosave file (cleanup deferred to next `save_document`). Satisfies "recover_from_autosave MCP tool replaces session state with autosave bytes" requirement.
- [x] 4.5 In Server.swift `open_document` and `openDocumentInternal` paths, add autosave detection: after document load, check `FileManager.default.fileExists(atPath: "<sourcePath>.autosave.docx")`; populate `autosave_detected: Bool` + `autosave_path: String?` fields in `SessionStateView` and the `get_session_state` / `get_document_session_state` tool responses. Satisfies "open_document detects existing autosave file" requirement.
- [x] 4.6 In Server.swift `saveDocument` handler success path AND `finalize_document` handler, after successful `persistDocumentToDisk`, delete `<sourcePath>.autosave.docx` if it exists via `try? FileManager.default.removeItem(...)`. Satisfies "Successful save_document cleans up .autosave.docx" scenario from "open_document supports autosave_every for periodic checkpoint" requirement.
- [x] 4.7 Verify all task 4.1 tests turn GREEN. Update `mcp/che-word-mcp/README.md` and `mcp/che-word-mcp/README_zh-TW.md`: add new "Autosave / Checkpoint / Recover (v3.6.0+)" section documenting `autosave_every` arg on `open_document`, `checkpoint` tool signature, `recover_from_autosave` tool signature, `autosave_detected` field on session state response. Bump tool count in README headers from current to current+2 (checkpoint + recover_from_autosave; `autosave_every` is just a new arg).
- [x] 4.8 Update `mcp/che-word-mcp/mcpb/manifest.json` to v3.6.0 (MINOR bump for new MCP tools) + `CHANGELOG.md` entry citing closes #37 + autosave + checkpoint + recover_from_autosave architecture; rebuild universal binary; repackage `.mcpb`; commit + push + `git tag v3.6.0` + gh release create with detailed notes + curl-upload binary + .mcpb; bump psychquant-claude-plugins to 3.6.0; commit + push + plugin marketplace sync + plugin update; run `/idd-close #37`.
- [x] 4.9 Confirms Decision: Ship as 4 sequential phases, each verifiable independently — at this point Phase 1 (v0.13.2 + v3.5.3), Phase 2 (v3.5.4), Phase 3 (v3.5.5), Phase 4 (v3.6.0) have all shipped via independent idd-implement → idd-verify → idd-close cycles on issues #36/#39/#38/#37 respectively. Run `/spectra-archive che-word-mcp-save-durability-stack` to archive the entire SDD; verify all 4 issue closing summaries posted; spec sync (5 ADDED requirements across `che-word-mcp-session-state-api` + 2 ADDED in new `ooxml-atomic-save`) merged into main `openspec/specs/`.
