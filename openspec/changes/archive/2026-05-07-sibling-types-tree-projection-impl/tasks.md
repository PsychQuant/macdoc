## 1. Implementation (per-type, parallelizable)

These three tasks each touch a single source file + its corresponding new test file. They are independent (different files, different test classes) and SHALL be dispatched in parallel via `[P]` markers.

- [x] 1.1 [P] Refactor `packages/ooxml-swift/Sources/OOXMLSwift/Models/Run.swift` applying these design decisions: **Decision 1: Same struct-with-class-reference pattern as Paragraph** — add `xmlNode: XmlNode?` stored property and `Run(xmlNode: XmlNode)` constructor; **Decision 2: `id: String?` derives from `XmlNode.stableID` with `lib:` UUID fallback** — add `run.id: String?` computed property; **Decision 3: Tree-walking getters for the primary content accessor only** — convert `text` and `properties` to mode-aware computed (tree-backed `text` walks `<w:t>` direct children; tree-backed `properties` returns `RunProperties()` Phase 1 stub); **Decision 4: Phase 1 stub setters mutate the tree directly** — implement tree-mutating `text` setter (replaces `<w:t>` children with one new `<w:t>X</w:t>` and calls `markDirty()`); **Decision 5: Identity-based `Equatable` for all five types** — replace auto-synthesized `Equatable` with mode-aware identity-vs-content; **Decision 7: Test gate convention identical to Paragraph** — write `packages/ooxml-swift/Tests/OOXMLSwiftTests/RunTreeProjectionTests.swift` GREEN-from-the-start with at least 7 tests (constructor, id via `w:id` attribute, id libraryUUID fallback, text getter, text setter mutates tree, setter marks dirty, identity equality). Verify all GREEN via `swift test --filter RunTreeProjectionTests`. Implements **Tree-backed sibling-type constructors**, **Sibling-type identity derived from XmlNode stableID with libraryUUID fallback**, **Tree-walking primary content getters** (Run.text), **Tree-mutating Run text setter (Phase 1 stub)**, **Identity-based equality for tree-backed sibling types**, and **Sibling-type RED scaffolds land GREEN** requirements (for Run only).

- [x] 1.2 [P] Refactor `packages/ooxml-swift/Sources/OOXMLSwift/Models/Table.swift` applying these design decisions to all three structs (`Table`, `TableRow`, `TableCell`): **Decision 1: Same struct-with-class-reference pattern as Paragraph** — each gains `xmlNode: XmlNode?` and `init(xmlNode:)`; **Decision 2: `id: String?` derives from `XmlNode.stableID` with `lib:` UUID fallback** — each gains `id: String?` computed; **Decision 3: Tree-walking getters for the primary content accessor only** — `Table.rows` walks `<w:tr>` children returning `TableRow(xmlNode:)`, `TableRow.cells` walks `<w:tc>` children returning `TableCell(xmlNode:)`, `TableCell.paragraphs` walks `<w:p>` children returning the v0.31.0 `Paragraph(xmlNode:)`, `TableCell.nestedTables` walks `<w:tbl>` children returning `Table(xmlNode:)`; **Decision 4: Phase 1 stub setters mutate the tree directly** — setters for `rows`/`cells`/`paragraphs`/`nestedTables` are ghost-writes to legacy buffers (no tree mutation in Phase 1); **Decision 5: Identity-based `Equatable` for all five types** — replace auto-synthesized `Equatable` on all three structs with mode-aware identity-vs-content; **Decision 7: Test gate convention identical to Paragraph** — write `packages/ooxml-swift/Tests/OOXMLSwiftTests/TableTreeProjectionTests.swift` GREEN-from-the-start with at least 9 tests (3 constructors × tree-backed/detached identity + tree-walking rows.count / cells.count / paragraphs propagation + identity equality across the three types). Verify all GREEN via `swift test --filter TableTreeProjectionTests`. Implements **Tree-backed sibling-type constructors**, **Sibling-type identity derived from XmlNode stableID with libraryUUID fallback**, **Tree-walking primary content getters** (Table/TableRow/TableCell paths), **Identity-based equality for tree-backed sibling types**, and **Sibling-type RED scaffolds land GREEN** requirements (for Table family).

- [x] 1.3 [P] Refactor `packages/ooxml-swift/Sources/OOXMLSwift/Models/Section.swift` applying these design decisions: **Decision 1: Same struct-with-class-reference pattern as Paragraph** — add `xmlNode: XmlNode?` stored property and `SectionProperties(xmlNode: XmlNode)` constructor; **Decision 2: `id: String?` derives from `XmlNode.stableID` with `lib:` UUID fallback** — add `sectionProperties.id: String?` computed; **Decision 5: Identity-based `Equatable` for all five types** — replace auto-synthesized `Equatable` with mode-aware identity-vs-content; **Decision 6: SectionProperties tree-backed mode is identity-only in Phase 1** — the 12+ structured fields (`pageSize`, `pageMargins`, `orientation`, `columns`, `docGrid`, `headerReferences`, `footerReferences`, `lineNumbers`, `verticalAlignment`, `pageNumberFormat`, `pageNumberStartValue`, `titlePageDistinct`, `sectionBreakType`) remain as legacy stored properties; tree-backed mode does NOT read them from `<w:sectPr>` children; reads return the `SectionProperties()` defaults; **Decision 7: Test gate convention identical to Paragraph** — write `packages/ooxml-swift/Tests/OOXMLSwiftTests/SectionPropertiesTreeProjectionTests.swift` GREEN-from-the-start with at least 5 tests (constructor, id libraryUUID fallback, identity equality, Phase 1 stub returns default `pageSize`, detached constructor still compiles). Note: **Decision 3: Tree-walking getters for the primary content accessor only** and **Decision 4: Phase 1 stub setters mutate the tree directly** do NOT apply to SectionProperties in this change because Decision 6 explicitly excludes structured-field tree-walking from Phase 1 scope. Verify all GREEN via `swift test --filter SectionPropertiesTreeProjectionTests`. Implements **Tree-backed sibling-type constructors**, **Sibling-type identity derived from XmlNode stableID with libraryUUID fallback**, **SectionProperties Phase 1 stub for structured fields**, **Identity-based equality for tree-backed sibling types**, and **Sibling-type RED scaffolds land GREEN** requirements (for SectionProperties).

## 2. Verification

These tasks run after §1 completes (depend on the implementation tasks above).

- [x] 2.1 Run the three new test classes via `swift test --filter RunTreeProjectionTests`, `swift test --filter TableTreeProjectionTests`, `swift test --filter SectionPropertiesTreeProjectionTests`. Assert each suite passes 100% with zero failures. Implements the **Sibling-type RED scaffolds land GREEN** requirement.

- [x] 2.2 Run the full `swift test` in `packages/ooxml-swift/`. Assert the total count is at least 928 (the v0.31.0 baseline) plus the new test methods from §1 (~21 more), with zero failures and only the pre-existing `.note` skip. Investigate any failure — most likely cause is identity-equality breaking a legacy test that compared two Reader-produced typed values; if so, that test path needs to detect tree-backed mode and fall back to content equality, OR (cleaner) the test path was never tree-backed in the first place and the failure is unrelated. Implements the **Public sibling-type API surface preserved during refactor** requirement (ooxml-swift internal regression gate).

## 3. Downstream regression gate

- [x] 3.1 After v0.31.1 is released (task 4.1 below), bump che-word-mcp's `Package.resolved` to pin ooxml-swift v0.31.1 via `cd /Users/che/Developer/macdoc/mcp/che-word-mcp && swift package update ooxml-swift`. Then run `swift test 2>&1 | tail -10` and assert the genuine regression result: 297 tests / 9 skipped / 0 failures (matching the v0.31.0 baseline). If failures appear, they reveal real regression in the sibling-type refactor that the in-repo unit tests missed. Document the test-pass count in the post-bump commit message. Implements the **che-word-mcp test suite passes against the refactored library** scenario of the **Public sibling-type API surface preserved during refactor** requirement.

## 4. Release prep + sibling change handoff

- [x] 4.1 Update `packages/ooxml-swift/CHANGELOG.md` with a `## [0.31.1] - YYYY-MM-DD` entry following the same KAC format the v0.31.0 entry uses. Sections: Added (Run/Table/TableRow/TableCell/SectionProperties tree-backed constructors + id accessors), Changed (mode-aware identity-based Equatable on all five types), Tests (3 new test files, 21+ new tests, full suite count, che-word-mcp downstream gate count), Out of scope (Settings refactor deferred per Decision 6 — no Settings struct exists; Reader rewiring; Phase 2 op-log routing; full SectionProperties tree-walking parsers deferred to follow-up `section-properties-tree-walking-impl`). Then commit + push to `main` with subject `v0.31.1: tree-backed Run / Table family / SectionProperties views (Phase 1 tasks 2.2-2.4)`. Tag `v0.31.0+` increment via `git tag -a v0.31.1 -m "..." && git push origin v0.31.1`. Create the GitHub Release via `gh release create v0.31.1 --title "v0.31.1 — tree-backed sibling typed views (Phase 1 tasks 2.2-2.4)" --notes-file <release-notes-path>` with notes mirroring the CHANGELOG entry.

- [x] 4.2 Mark `word-aligned-state-sync` Phase 1 tasks 2.2, 2.3, and 2.4 as done in that change's `tasks.md` via three separate `spectra task done --change word-aligned-state-sync <ordinal>` calls. Find the ordinals via `awk '/^- \[/ {n++; print n, }' openspec/changes/word-aligned-state-sync/tasks.md | grep -E '2\.[234]'`. Task 2.5 (Settings refactor) remains pending per Decision 6 / Non-Goals — record a follow-up Spectra change name `section-properties-tree-walking-impl` (for SectionProperties full tree-walking) plus `settings-extraction-impl` (for the missing Settings struct) in the post-merge handoff comment.
