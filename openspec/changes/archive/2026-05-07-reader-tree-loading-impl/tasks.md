## 1. Implementation (sequential — same files compound)

These four tasks are sequential because they touch overlapping regions of `Document.swift` and `DocxReader.swift`. No `[P]` markers — parallel dispatch on the same file invites merge conflicts.

- [x] 1.1 Apply **Decision 1: `xmlTrees` is a public read-only stored property on WordDocument** and **Decision 5: Equatable on WordDocument needs to ignore xmlTrees** to `packages/ooxml-swift/Sources/OOXMLSwift/Models/Document.swift`. Add `public internal(set) var xmlTrees: [String: XmlTree] = [:]` stored property (positioned near the existing `preservedArchive` and `modifiedParts` fields since it follows the same internal-set pattern). Add `public func partTree(at partPath: String) -> XmlTree?` accessor that returns `xmlTrees[partPath]`. Replace the auto-synthesized `WordDocument.Equatable` with a custom `static func ==` that explicitly enumerates every stored field EXCEPT `xmlTrees`, `preservedArchive`, and `modifiedParts` (the three round-trip-state fields that should not participate in content equality). Run `swift build` in `packages/ooxml-swift/` to verify compile-clean before continuing. Implements the **WordDocument carries XmlTree per loaded OOXML part** requirement.

- [x] 1.2 Apply **Decision 2: DocxReader populates xmlTrees during the existing per-part read flow** to `packages/ooxml-swift/Sources/OOXMLSwift/IO/DocxReader.swift`. At every call site inside `DocxReader.read(from:)` that loads a primary OOXML part via `Data(contentsOf: <partURL>)`, ALSO call `XmlTreeReader.parse(thatData)` and assign the result to `document.xmlTrees[partPath]`. Primary parts in scope: `word/document.xml`, `word/styles.xml`, `word/numbering.xml`, `word/settings.xml`, `word/comments.xml`, `word/footnotes.xml`, `word/endnotes.xml`, every `word/header*.xml`, every `word/footer*.xml`, every `word/customXml/*.xml`. Relationship parts (`*.rels`, `[Content_Types].xml`) SHALL NOT be loaded into `xmlTrees` per Decision 2. If `XmlTreeReader.parse` throws on any part, propagate the throw — do NOT swallow. Run `swift build` after each call-site addition to verify compile-clean. Implements the **DocxReader populates xmlTrees during read** requirement.

- [x] 1.3 Apply **Decision 3: `wireTreeBackedViews: Bool = false` is the new opt-in parameter** and **Decision 4: Body-level wiring only; nested structure propagates via v0.31.1 computed accessors** to `packages/ooxml-swift/Sources/OOXMLSwift/IO/DocxReader.swift`. Update the `read(from: URL)` signature to `read(from: URL, wireTreeBackedViews: Bool = false)`. When `wireTreeBackedViews: true`, after the typed-model construction completes, walk `xmlTrees["word/document.xml"].root.children` to find the `<w:body>` element. Iterate `<w:body>`'s direct children in source order, tracking a position cursor `i` into `document.body.children`. For each `<w:body>` direct child that is `<w:p>` and `body.children[i]` is `.paragraph(var p)`: set `p.xmlNode = thatNode`, write back the updated case via `body.children[i] = .paragraph(p)`. Same pattern for `<w:tbl>` ↔ `.table(var t)`. Skip `<w:sectPr>` (no body.children mapping). Defensively skip the body.children entry when types do not match (cursor stays in sync). Run `swift build` to verify compile-clean. Implements the **Default Reader behavior preserves v0.31.1 detached typed-view semantics** requirement (default-false path) AND the **wireTreeBackedViews opt-in mode wires body-level Paragraph and Table** requirement (opt-in path).

- [x] 1.4 Apply **Decision 6: Test fixture choice — use the existing corpus in `Tests/.../Fixtures`** and **Decision 7: Test gate convention identical to v0.31.1** to create `packages/ooxml-swift/Tests/OOXMLSwiftTests/ReaderTreeLoadingTests.swift` (NEW file). Use the existing `CorpusFixtureBuilder` to load the four corpus fixtures (`multi-section-thesis.docx`, `vml-rich.docx`, `cjk-settings.docx`, `comment-anchored.docx`). Include at least these 7 test methods (one per Scenario in spec.md): `testReader_xmlTreesPopulatedForDocumentXml`, `testReader_xmlTreesPopulatedForAllParts`, `testReader_partTreeReturnsNilForUnknownPath`, `testWordDocumentEqualityIgnoresXmlTrees`, `testReader_defaultModeKeepsBodyParagraphsDetached`, `testReader_wireTreeBackedViewsSetsBodyParagraphXmlNode`, `testReader_wireTreeBackedViewsSetsBodyTableXmlNode`. NO `#if false` gate per Decision 7 — tests land GREEN-from-the-start. Run `swift test --filter ReaderTreeLoadingTests` and verify all pass. Implements the **ReaderTreeLoadingTests pinned coverage** requirement.

## 2. Verification

- [x] 2.1 Run `swift test --filter ReaderTreeLoadingTests` and assert all 7+ tests pass. Then run the full `swift test` in `packages/ooxml-swift/` and assert the total count is at least 950 (the v0.31.1 baseline) plus the new test methods from §1.4 (~7 more), with zero failures and only the pre-existing `.note` skip. Investigate any failure — most likely cause is the custom `WordDocument.Equatable` change in task 1.1 breaking a legacy test that compared two Reader-produced documents; if so, the test's expectation needs review (those documents really should remain equal even with distinct `xmlTrees`). Implements the regression-gate side of the **WordDocument carries XmlTree per loaded OOXML part** requirement and the **Default Reader behavior preserves v0.31.1 detached typed-view semantics** requirement.

## 3. Downstream regression gate

- [x] 3.1 After v0.31.2 is released (task 4.1 below), bump che-word-mcp's `Package.resolved` to pin ooxml-swift v0.31.2 via `cd /Users/che/Developer/macdoc/mcp/che-word-mcp && swift package update ooxml-swift`. Then run `swift test 2>&1 | tail -10` and assert the genuine regression result: 297 tests / 9 skipped / 0 failures (matching the v0.31.1 baseline). Investigate any failure — che-word-mcp call sites do NOT pass `wireTreeBackedViews:`, so they hit the default-mode path; failures indicate the default-mode behavior drifted (e.g., `xmlTrees` population leaking into observable behavior somewhere). Document the test-pass count in the post-bump commit message. Implements the **che-word-mcp regression gate stays green against the wired Reader** requirement.

## 4. Release prep + sibling change handoff

- [x] 4.1 Update `packages/ooxml-swift/CHANGELOG.md` with a `## [0.31.2] - YYYY-MM-DD` entry following the same KAC format the v0.31.0 and v0.31.1 entries use. Sections: Added (`WordDocument.xmlTrees: [String: XmlTree]` + `partTree(at:)` accessor; `DocxReader.read(from:wireTreeBackedViews:)` parameter; opt-in body-level wiring); Changed (custom `WordDocument.Equatable` ignoring `xmlTrees` / `preservedArchive` / `modifiedParts`); Tests (new `ReaderTreeLoadingTests` with 7 tests; full suite count; che-word-mcp downstream gate count); Out of scope (nested-structure wiring auto-propagates via v0.31.1 computed accessors; header/footer/footnote/endnote/comment typed-view wiring deferred to follow-up `header-footer-tree-wiring-impl`; legacy parser path removal deferred to Phase 5 v1.0.0; op-log routing deferred to Phase 2 v0.32.0). Then commit + push to `main` with subject `v0.31.2: Reader xmlTrees + opt-in tree-backed wiring (Phase 1 task 2.6)`. Tag via `git tag -a v0.31.2 -m "..." && git push origin v0.31.2`. Create the GitHub Release via `gh release create v0.31.2 --title "v0.31.2 — Reader xmlTrees + opt-in tree-backed wiring (Phase 1 task 2.6)" --notes-file <release-notes-path>` with notes mirroring the CHANGELOG entry.

- [x] 4.2 Mark `word-aligned-state-sync` Phase 1 task 2.6 as done in that change's `tasks.md`. Find the ordinal via `python3 -c "import re; n=0; [print(n) or exit() for line in open('openspec/changes/word-aligned-state-sync/tasks.md') if (n:=n+1) and re.match(r'^- \[', line) and re.search(r'2\.6 ', line)]"`, then run `spectra task done --change word-aligned-state-sync <ordinal>`. Tasks 2.7-2.10 (per-parser unknown-child preservation + revision tree round-trip + revision typed-view backed by tree) remain pending — record follow-up Spectra change names `reader-revision-tree-impl` (for tasks 2.8-2.10) and `reader-unknown-child-coverage-impl` (for task 2.7) in the post-archive handoff.
