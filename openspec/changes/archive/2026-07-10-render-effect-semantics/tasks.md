## 1. Phase 1 — Geometry measurement (harness extension)

- [x] 1.1 `RenderGeometry` test-target helper + `RenderGeometryTests`, TDD order: first commit two tiny PDF fixtures generated via CoreGraphics text drawing (one page with 5 lines at a known 14pt baseline distance; one page with a single line) under Tests/OOXMLSwiftTests/Fixtures/, write failing tests pinning `pageCount(pdf:)`, `pageBox(pdf:page:)`, `lineBoxes(pdf:page:)` count, `medianLinePitch(pdf:page:)` value within measurement tolerance, and nil line pitch on the single-line page; then implement the helper on PDFKit (`PDFPage.selectionsByLine()` bounds, `bounds(for: .mediaBox)`) until green. No `RUN_WORD_INTEGRATION` gate anywhere in this task. → satisfies «Geometry measurement extends the harness beyond pixel ratios» · Decisions 2, 6. Verify: `swift test --filter RenderGeometryTests` green on a machine without Word.

## 2. Phase 2 — Render-effect registry and perturbation probes

- [x] 2.1 Registry scaffold + probe plumbing: create docs/render-effect-registry.md with the 7 starter rows (docGridLinePitch, spacingBefore, spacingAfter, spacingLine-auto, indentFirstLineChars, sizeHalfPoints, pageSize/margins), every row initially status `unverified`, columns per the design Implementation Contract (field, observable, predicted direction, expected magnitude + unit conversion, tolerance, evidence, probe name, status). Create RenderEffectProbeTests.swift with the shared probe harness: builds baseline/perturbed docx via `WordDocument.emptyAuthoringDocument()` + `apply(operations:)`, renders both through the existing VisualDiffTests Word AppleScript bridge, all I/O pinned to the granted `~/.cache/ooxml-swift-visual-diff` directory via a helper constant, loud XCTSkip without gate or Word. → satisfies «Rendering understanding is probe-verified registry knowledge» · Decisions 1, 3, 4. Verify: registry file exists with 7 unverified rows; probe harness compiles; ungated run reports skips.
- [x] 2.2 Probe: `docGridLinePitch` — baseline section `docGridType: "lines"`, `docGridLinePitch: 360`, ≥5 lines of body text; perturbed 480; assert measured median line pitch increases by 6.0 pt (= 120/20) within Decision 5 tolerance (±10% or ±1.0 pt, whichever larger); on pass flip the registry row to `verified` with measured value + date + Word version. → satisfies «Perturbation probes verify predicted rendering effects» (line-pitch scenario, exact spec example values) · Decisions 4, 5. Verify: `RUN_WORD_INTEGRATION=1 swift test --filter RenderEffectProbeTests` shows the probe passing; registry row updated.
- [x] 2.3 Probes: `spacingBefore` and `spacingAfter` — two-paragraph fixture; perturb one field per probe (+240 twips = +12 pt); observable = vertical gap between last line box of paragraph 1 and first line box of paragraph 2; assert gap grows by 12 pt within tolerance; update both registry rows on pass. → satisfies «Perturbation probes verify predicted rendering effects» · Decision 5. Verify: both probes green under the gate; rows `verified`.
- [x] 2.4 Probe: `spacingLine` with rule `auto` — single multi-line paragraph, baseline `spacingLine: 240` (single), perturbed 360 (1.5×); assert median line pitch scales by ×1.5 within tolerance; update registry row. → satisfies «Perturbation probes verify predicted rendering effects» · Decision 5. Verify: probe green under the gate; row `verified`.
- [x] 2.5 Probe: `indentFirstLineChars` — CJK body paragraph, baseline no indent, perturbed `indentFirstLineChars: 100` (1 char); observable = x-offset of the first line box relative to subsequent line boxes; assert first line shifts right by approximately one character advance (predicted = run font size in pt) within tolerance; if `selectionsByLine()` proves unstable on grid-snapped CJK text, leave the row `unverified` with the failure mode documented per the honest-registry scenario — do NOT loosen tolerance. → satisfies «Perturbation probes verify predicted rendering effects» + «Rendering understanding is probe-verified registry knowledge» (unverifiable scenario) · Decision 5, risk table. Verify: probe green with row `verified`, OR row `unverified` with documented failure mode and the probe asserting the documented behavior.
- [x] 2.6 Probe: `sizeHalfPoints` — multi-line paragraph, baseline `sizeHalfPoints: 21` (10.5 pt), perturbed 42 (21 pt); observable = median line-box height and line pitch; assert both grow (direction) and pitch approximately doubles within tolerance; update registry row. → satisfies «Perturbation probes verify predicted rendering effects» · Decision 5. Verify: probe green under the gate; row `verified`.
- [x] 2.7 Probe: page size and margins — baseline A4 (`pageWidth: 11906`, `pageHeight: 16838`) with default margins; perturbed `marginTop: +567` twips (+1 cm); observables = page box (must be unchanged pt-for-pt from `pageWidth/20 × pageHeight/20`) and y-position of the first line box (must shift down by 28.35 pt within tolerance); update registry row. → satisfies «Perturbation probes verify predicted rendering effects» · Decision 5. Verify: probe green under the gate; row `verified`.

## 3. Phase 3 — Slotted-rebuild render acceptance and close-out

- [x] 3.1 `RealTemplateUpgradeTests` scenario (d): gated behind BOTH `RUN_WORD_INTEGRATION=1` and `MACDOC_TEMPLATE_DIR` (loud skip if either absent); reverse the JPA template, export with one op-level slot, substitute sentinel text of comparable length, execute, render reference and rebuilt docx through Word; assert equal page count, equal page box on every page, substituted-page median line pitch within Decision 5 tolerance of the reference page, and pixel-ratio threshold pass on every untouched page. → satisfies «Slotted rebuilds carry render-level acceptance» · Decision 7. Verify: dual-gated run green on the maintainer machine; single-gate runs skip loudly naming the missing gate.
- [x] 3.2 Close-out: add a render-semantics section to docs/format-alignment-baselines.md (registry summary table snapshot: N verified / M unverified with evidence values); add the layer-3 note to CLAUDE.md's `word reverse` description (spellable ≠ understood; registry is the understanding ledger); full regression `swift test` in packages/ooxml-swift and in macdoc root both green. → satisfies «Rendering understanding is probe-verified registry knowledge» (documentation trace) · Migration plan (test-target + docs only, no release). Verify: both suites green; registry, baselines, CLAUDE.md consistent on verified-entry count.
