> **Structure**: one change, four phases per **Decision 4 (one change, four phases)** — hard ordering: metrics floor before extraction before slots.
> **Traceability**: each task tags the spec requirement it satisfies («Title») and the governing design decision (Decision N).

### Design decision → task index

- Decision 1: Stage C exemption — Stage B is the final acceptance → tasks 1.3, 4.2
- Decision 2: dual-track — raw channel is the floor, DSL coverage is the score → tasks 1.1, 1.2, 1.6, 3.3
- Decision 3: typed-DSL byte equality is achieved via write-back projection, not canonical exemptions → tasks 2.1, 2.2, 2.3, 2.4, 2.5
- Decision 4: one change, four phases → tasks 3.1, 4.1, 4.4
- Decision 5: fixture policy — private real templates env-gated, one synthetic committed → tasks 1.4, 1.5, 3.2
- Decision 6: visual diff via Word AppleScript PDF export, gated → task 4.3

## 1. Phase A — Metrics foundation + raw-channel floor

- [x] 1.1 Implement `PartFidelity` (packages/ooxml-swift/Sources/OOXMLSwift/Transcode/PartFidelity.swift): per-part byte-diff (Stage A verdict per part, Stage B verdict for the set, first-divergence offset on mismatch) and DSL/raw byte-split accounting. → satisfies «DSL-form coverage measurement» + «Dual-track acceptance — byte-equal floor and DSL coverage score» · Decision 2 (dual-track — raw channel is the floor, DSL coverage is the score). Verify: unit tests cover equal / differing / missing-part cases with exact offsets.
- [x] 1.2 Implement the all-parts raw channel in ScriptTranscoder: script sections that carry every source XML part verbatim (base64 or fenced raw blocks with byte-exact restore), emitted by `word reverse` and honored on execute. → satisfies «All-parts raw channel» + «Dual-track acceptance — byte-equal floor and DSL coverage score» · Decision 2 (dual-track — raw channel is the floor, DSL coverage is the score). Verify: `testSiblingPartRoundTripsVerbatim` per spec scenario — styles.xml byte-equal after rebuild.
- [x] 1.3 Wire Stage A/B acceptance into a pipeline test: reverse → execute → PartFidelity over the synthetic template; byte-equal green from this task onward (the never-regress floor). Container-normalize before comparison so zip entry ordering is ignored. → satisfies «Single-path rebuild pipeline» + «Stage C zip-container equality is out of contract» · Decision 1 (Stage C exemption — Stage B is the final acceptance). Verify: `FormatAlignmentBaselineTests.testStageBPassesOnRawChannel`.
- [x] 1.4 [P] Build `CJKTemplateFixtureGenerator` (Tests/MacDocCLITests/Fixtures/ mirror in ooxml-swift tests): synthetic two-section CJK template — second section `w:cols num="2"`, eastAsia fonts, ≥8 styles, settings surface. → satisfies «Template fixture policy» · Decision 5 (fixture policy — private real templates env-gated, one synthetic committed). Verify: generated file parses and features assert present.
- [x] 1.5 [P] Add `MACDOC_TEMPLATE_DIR` env-gate helper + XCTSkip fallback; record the real-template baseline (90_template_ja) coverage numbers in the change's baseline doc when the gate is present. → satisfies «Template fixture policy» · Decision 5 (fixture policy — private real templates env-gated, one synthetic committed). Verify: suite passes with and without the env var.
- [x] 1.6 Add `macdoc word reverse --coverage` flag printing the per-part DSL/raw split and aggregate %. → satisfies «DSL-form coverage measurement» · Decision 2 (dual-track — raw channel is the floor, DSL coverage is the score). Verify: CLI e2e on the synthetic template shows a parsable report.

## 2. Phase B — Five-layer extraction (raw→DSL upgrades)

- [x] 2.1 Additive payload extensions: RunPayload (font ascii/eastAsia, size, underline, vertAlign), ParagraphPayload (spacing, indent, alignment, numPr ref), new SectionPayload — JSONL codec + reducer stamping, field names checked against envelope keys. → satisfies «Format-payload additive extensions» · Decision 3 (typed-DSL byte equality via write-back projection, not canonical exemptions). Verify: old-sidecar decode test + field-for-field wire round-trip test per spec scenarios.
- [x] 2.2 Run-level reverse extraction: typed rPr projection in reverse + DSL emission + reducer rebuild; upgrade accepted only if rPr rebuilds byte-equal, else stays raw. → satisfies «Reverse extraction covers the five format layers» · Decision 3 (typed-DSL byte equality via write-back projection, not canonical exemptions). Verify: `testCJKRunFormattingSurvivesDSLChannel` per spec scenario; Stage A/B stay green; coverage strictly increases.
- [x] 2.3 [P] Paragraph-level reverse extraction (spacing/indent/alignment/numPr) with the same byte-equal upgrade rule. → satisfies «Reverse extraction covers the five format layers» · Decision 3 (typed-DSL byte equality via write-back projection, not canonical exemptions). Verify: pPr byte-equal test on synthetic template; coverage increases.
- [x] 2.4 Section-level reverse extraction: SectionPayload reverse + Section DSL emission (page size/margins/columns); two-column scenario per spec. → satisfies «Reverse extraction covers the five format layers» · Decision 3 (typed-DSL byte equality via write-back projection, not canonical exemptions). Verify: `testTwoColumnSectionRoundTrips`; Stage A/B green.
- [x] 2.5 Table extraction to the DSL channel where byte-equal permits (leveraging the Table DSL types shipped compile-level in wass 5.5), raw fallback otherwise. → satisfies «Reverse extraction covers the five format layers» · Decision 3 (typed-DSL byte equality via write-back projection, not canonical exemptions). Verify: table-bearing synthetic fixture round-trips; coverage report shows table class attribution.

## 3. Phase C — Full-fidelity default

- [x] 3.1 Make full-fidelity (all-parts) the `word reverse` default with `--paragraphs-only` opt-out per the design migration plan; update CLI help + CLAUDE.md command table. → satisfies «All-parts raw channel» · Decision 4 (one change, four phases). Verify: default run on synthetic template passes Stage B; opt-out reproduces old behavior.
- [x] 3.2 [P] Coverage baseline documentation: record synthetic + (env-gated) real-template coverage trajectory in docs/format-alignment-baselines.md with per-phase numbers. → satisfies «DSL-form coverage measurement» · Decision 5 (fixture policy — private real templates env-gated, one synthetic committed). Verify: doc exists with Phase A and Phase B measured values.
- [x] 3.3 Guard test: every raw→DSL upgrade class has a regression pin asserting Stage B stays green when that class is exercised (one parameterized test over upgrade classes). → satisfies «Dual-track acceptance — byte-equal floor and DSL coverage score» · Decision 2 (dual-track — raw channel is the floor, DSL coverage is the score). Verify: test enumerates the upgraded classes and fails if a class regresses to non-byte-equal.

## 4. Phase D — Content slots + visual diff

- [x] 4.1 Slot designation input: `word reverse --slot <name>=<paragraph-id>` (repeatable) producing a parameterized script (Swift function parameters per design Q2 working answer). → satisfies «Strict template mode via named content slots» + «Slot designation is explicit in strict mode» · Decision 6 (visual diff via Word AppleScript PDF export, gated) is a sibling; slots governed by Decision 4 (one change, four phases). Verify: title/body scenario test — new content in designated positions, all other bytes as extracted.
- [x] 4.2 No-designation invariant: script without slots reproduces the reference byte-equal (Stage B). → satisfies «Slot designation is explicit in strict mode» · Decision 1 (Stage C exemption — Stage B is the final acceptance). Verify: regression test on synthetic template.
- [x] 4.3 Visual-diff harness: Word AppleScript PDF export + PDFKit/CoreGraphics page render + per-page pixel-diff ratio with threshold; loud skip without gate/Word. → satisfies «Gated visual regression harness» + «Native conversion channel only» · Decision 6 (visual diff via Word AppleScript PDF export, gated). Verify: identical-documents-pass + layout-drift-caught scenarios (drop the two-column sectPr to force a failure) under RUN_WORD_INTEGRATION=1.
- [x] 4.4 [P] End-to-end acceptance run: synthetic + real (env-gated) templates through reverse → rebuild → Stage B → visual diff; record final coverage numbers; ooxml-swift release tag per migration plan. → satisfies «Single-path rebuild pipeline» + «Native conversion channel only» · Decision 4 (one change, four phases). Verify: full suite green including gated tests on the maintainer machine.
