## 1. Phase 1 — ooxml-swift v0.13.0 (dirty tracking + fileName preservation)

- [x] 1.1 In `packages/ooxml-swift/Sources/OOXMLSwift/Models/Document.swift`, implement Decision: Dirty tracking is `Set<String>` of OOXML part paths, not typed enum — add `internal var modifiedParts: Set<String> = []` to `WordDocument`; expose `public var modifiedPartsView: Set<String>` read-only accessor; add `public mutating func markPartDirty(_ partPath: String)`; verify Equatable manual impl excludes modifiedParts (matching archiveTempDir exclusion pattern from v0.12.0); satisfies "WordDocument tracks modified OOXML parts via Set of paths" requirement
- [x] 1.2 [P] In `packages/ooxml-swift/Sources/OOXMLSwift/Models/Header.swift`, implement Decision: `Header` and `Footer` gain `originalFileName: String?`; `fileName` becomes `originalFileName ?? type-based-default` for the Header struct half — add `public var originalFileName: String?` field; modify `fileName` computed property to return `originalFileName ?? <existing type-based default switch>`; verify `Header.withText` / `Header.withPageNumber` initializers leave originalFileName nil; satisfies "Header struct preserves originalFileName from source archive" requirement
- [x] 1.3 [P] In `packages/ooxml-swift/Sources/OOXMLSwift/Models/Footer.swift`, mirror Header changes for Footer struct — add `public var originalFileName: String?` field; modify `fileName` computed property to return `originalFileName ?? <existing default>`; satisfies "Footer struct preserves originalFileName from source archive" requirement
- [x] 1.4 In `packages/ooxml-swift/Sources/OOXMLSwift/IO/DocxReader.swift`, populate `Header.originalFileName` and `Footer.originalFileName` from each header/footer relationship's `Target` attribute when constructing typed instances during parse; clear `result.modifiedParts` to empty set as the final step before `transferOwnership = true; return result`; satisfies "Multi-instance same-type headers/footers preserve distinct fileNames" requirement (verified for documents like NTPU thesis with 6 default headers all loaded with distinct originalFileNames)
- [x] 1.5 In `packages/ooxml-swift/Sources/OOXMLSwift/Models/Document.swift`, instrument every mutating method to call `self.modifiedParts.insert(<corresponding part path>)`: appendParagraph/insertParagraph/updateParagraph/deleteParagraph/replaceText/insertText/insertTable/updateCell/deleteTable/mergeCells/all table row/column mutators → `"word/document.xml"`; addStyle/updateStyle/deleteStyle → `"word/styles.xml"`; addHeader/updateHeader → `"word/\(header.fileName)"`; addFooter/updateFooter → `"word/\(footer.fileName)"`; image add → `"word/media/\(image.fileName)"` + `"[Content_Types].xml"`; comment add/update/reply/resolve/delete → `"word/comments.xml"` + (if extended) `"word/commentsExtended.xml"`; footnote add/delete → `"word/footnotes.xml"`; endnote add/delete → `"word/endnotes.xml"`; properties set → `"docProps/core.xml"`; numbering add → `"word/numbering.xml"`; satisfies "Mutating methods on WordDocument insert corresponding part paths into modifiedParts" requirement
- [x] 1.6 In `packages/ooxml-swift/Sources/OOXMLSwift/IO/DocxWriter.swift`, implement Decision: Overlay-mode writers SKIP for parts not in `modifiedParts`; scratch mode unchanged — refactor `writeAllParts(_, to:, overlayMode:)` to wrap every typed-part writer in dirty check; satisfies "DocxWriter overlay mode skips writers for parts not in modifiedParts" requirement + "writeFontTable in overlay mode is skip-by-default to preserve original font declarations" requirement + "writeStyles in overlay mode is skip-by-default to preserve theme font references" requirement + "writeHeader and writeFooter in overlay mode are per-instance dirty-checked" requirement; add helper `private static func hasNewTypedParts(_ document: WordDocument, originalCT: String) -> Bool`; gate `writeContentTypes` on `modifiedParts.contains("[Content_Types].xml") || hasNewTypedParts(...)` to satisfy "writeContentTypes overlay mode includes overrides for typed parts the typed model now references" requirement + "ContentTypesOverlay handles new typed parts via hasNewTypedParts trigger" requirement; gate `writeDocumentRelationships` on `modifiedParts.contains("word/_rels/document.xml.rels") || hasNewTypedRels(...)`; remove top-level `writeRelationships` from overlay-mode (read-only file); preserve scratch-mode behavior unchanged
- [x] 1.7 [P] Implement Decision: Test fixture is committed multi-header docx, not NTPU thesis — hand-craft `packages/ooxml-swift/Tests/OOXMLSwiftTests/Fixtures/multi-header-footer.docx` (~30KB) containing 6 default headers (each with unique recognizable text + watermark VML containing `<v:shape id="PowerPlusWaterMarkObject1..6"` + `<v:textpath string="浮水印N">`), 4 default footers (with footer3.xml containing three-segment `<w:fldChar>` + `<w:instrText>PAGE</w:instrText>` + `<w:fldChar>` field), 1 `<w15:person>` with full presenceInfo `userId="S::test@example.com::aaaaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee"` `providerId="AD"`, fontTable with 13 entries (Calibri/Calibri Light/TNR/DFKai-SB/華康中楷體/PMingLiU/Microsoft JhengHei/Cambria Math/Symbol/Wingdings/Courier New/Cambria/Malgun Gothic); document build procedure in `Tests/OOXMLSwiftTests/Fixtures/README.md`
- [x] 1.8 [P] Create `packages/ooxml-swift/Tests/OOXMLSwiftTests/MarkDirtyCoverageTests.swift` enumerating one test case per WordDocument mutating method; each test loads fresh document, calls method, asserts expected path in modifiedPartsView; include grep-based audit comment listing all currently audited mutators; satisfies "MarkDirtyCoverageTests enumerates and validates every WordDocument mutating method" requirement; also add a smoke test calling `markPartDirty(...)` directly to satisfy "External callers SHALL mark archive parts dirty when writing directly to archiveTempDir" requirement
- [x] 1.9 [P] Create `packages/ooxml-swift/Tests/OOXMLSwiftTests/MultiHeaderFooterFixtureTests.swift` exercising the 30KB fixture: (a) verify `doc.headers.count == 6` and 6 distinct fileNames after read; (b) no-op round-trip preserves all 26 ZIP entries byte-equal at file-content level; (c) modifying only one header preserves the other 5; (d) modifying styles preserves headers/footers/fontTable/theme; (e) modifying theme via markPartDirty + direct write preserves all other parts including 13 fontTable entries
- [x] 1.10 Update `packages/ooxml-swift/CHANGELOG.md` with v0.13.0 entry: dirty tracking via modifiedParts, Header/Footer originalFileName, overlay-mode skip-when-not-dirty, fixture-driven byte-preservation tests; run `swift test` to confirm all 327+ existing tests + new fixture tests pass; commit, push, tag v0.13.0, gh release create v0.13.0 — completes Phase 1

## 2. Phase 2 — che-word-mcp v3.5.0 (mark-dirty wiring + #32/#33/#34 fixes + release)

- [x] 2.1 In `mcp/che-word-mcp/Package.swift`, bump `ooxml-swift` dep to `^0.13.0`; run `swift package update`; verify all 68 existing che-word-mcp tests still pass against new ooxml-swift version; this implements Decision: Single SDD change covers all 4 issues; no split — reusing the same dep bump for #23 round-2 + #32 + #33 + #34 fixes
- [x] 2.2 In `mcp/che-word-mcp/Sources/CheWordMCP/Server.swift`, modify all archive-write helpers to mark dirty: `writeThemeXML(...)` adds `doc.markPartDirty("word/theme/theme1.xml")`; `writeArchivePart(docId, partPath, content)` adds `doc.markPartDirty(partPath)`; verify `documentDirtyState[docId] = true` AND `doc.modifiedParts.insert(...)` both happen in every archive-write path; audit list_headers / get_header / delete_header / list_footers / get_footer / delete_footer / list_watermarks / get_watermark / list_people / add_person / update_person / delete_person / get_endnote / update_endnote / get_footnote / update_footnote / get_web_settings / update_web_settings handlers — for each one that mutates archiveTempDir, add markPartDirty (this wires up the External callers mark-dirty contract from ooxml-roundtrip-fidelity)
- [x] 2.3 In `mcp/che-word-mcp/Sources/CheWordMCP/Server.swift`, rewrite `extractPeople` to parse `<w15:person>...</w15:person>` outer block + `<w15:presenceInfo>` child element using multi-line regex or XMLDocument; extract `userId="S::EMAIL::GUID"` triple-segment via `split(separator: "::", maxSplits: 3)`; extract `w15:providerId` and optional `w15:color` attributes; rewrite `listPeople` handler to return JSON entries with `person_id` (GUID, fallback to author), `display_name_id` (= author legacy), `display_name`, `email`, `color`, `provider_id`; satisfies "list_people parses w15:presenceInfo child element fully" requirement + "list_people returns dual identity fields person_id (GUID) and display_name_id (author)" requirement; implements Decision: `#34` person_id GUID is BREAKING with backward-compat in v3.5.0
- [x] 2.4 In `mcp/che-word-mcp/Sources/CheWordMCP/Server.swift`, modify `updatePerson` and `deletePerson` handlers to accept either `person_id` (GUID) or `display_name_id` (legacy author string) — handler tries both identifier interpretations against parsed people entries before erroring; preserve existing single-arg signature (`person_id` parameter accepts either form); satisfies "update_person and delete_person accept either person_id (GUID) or display_name_id (legacy author)" requirement
- [x] 2.5 In `mcp/che-word-mcp/Sources/CheWordMCP/Server.swift`, strengthen `headerHasWatermark(xml)` regex to handle multi-instance VML shape IDs (`PowerPlusWaterMarkObject1`-`6`); verify NTPU-style fixture content with `<v:shape id="PowerPlusWaterMarkObject2" o:spt="136"...>` triggers true; strengthen `footerHasPageNumber(xml)` to detect three-segment `<w:fldChar w:fldCharType="begin"/>` + `<w:instrText[^>]*>\s*PAGE\b` + `<w:fldChar w:fldCharType="end"/>` pattern via three-step regex check (begin char + instrText with PAGE + end char in same paragraph); preserve existing `<w:fldSimple w:instr="...PAGE...">` matching; satisfies "list_watermarks reads each typed header's distinct fileName" requirement (now lookup hits actual files) + "list_headers.has_watermark reflects per-header VML detection" requirement + "list_footers.has_page_number detects three-segment fldChar PAGE field pattern" requirement + "get_header / get_footer / delete_header / delete_footer address each header/footer by distinct fileName" requirement
- [x] 2.6 [P] Create `mcp/che-word-mcp/Tests/CheWordMCPTests/PeoplePresenceInfoTests.swift` with fixtures: (a) presenceInfo with full `userId="S::email::guid"` → assert person_id=guid, display_name_id=author, email=email, provider_id=AD; (b) presenceInfo without GUID → assert person_id falls back to author; (c) update_person via GUID path; (d) update_person via display_name_id path; (e) delete_person via either identifier
- [x] 2.7 [P] Update `mcp/che-word-mcp/Tests/CheWordMCPTests/HeadersFootersToolsTests.swift` adding multi-instance scenarios: build fixture with 3 default headers each with distinct content + 1 with watermark; assert list_headers returns 3 distinct entries; list_watermarks returns 1; modifying header2 preserves header1 + header3 byte content; assert footer with three-segment PAGE field returns has_page_number=true (currently failing scenario from #33)
- [x] 2.8 Update `mcp/che-word-mcp/Tests/CheWordMCPTests/Phase2BCSmokeTests.swift` adding presenceInfo + dirty-tracking scenarios: (a) update_web_settings call → after save, only webSettings.xml changed, fontTable preserved; (b) addPerson → after save, only people.xml changed, all other parts preserved; tests run on a Reader-loaded fixture not initializer-built (so archiveTempDir engaged)
- [x] 2.9 Update `mcp/che-word-mcp/CHANGELOG.md` with v3.5.0 entry: round-2 fix for #23 (true byte-preservation via dirty tracking), closes #32 #33 #34, dual identity person_id with v4.0.0 deprecation note; include "Migration: person_id semantic change" section satisfying "people-tools document v3.4.0 → v4.0.0 backward-compat migration" requirement; bump `mcpb/manifest.json` version to 3.5.0; run swift build -c release; copy binary to mcpb/server/CheWordMCP and ~/bin/CheWordMCP; rebuild .mcpb; commit, push, tag v3.5.0, gh release create v3.5.0; upload BOTH .mcpb AND standalone CheWordMCP binary to release (per the v3.2.0+ regression we caught last cycle); the entire `che-word-mcp-true-byte-preservation` work is now complete and #23 round-2 + #32 + #33 + #34 ready for /idd-verify + /idd-close
