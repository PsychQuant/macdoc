## 1. ooxml-swift v0.11.0 — Add MathAccent

- [x] 1.1 In `packages/ooxml-swift/Sources/OOXMLSwift/Models/MathComponent.swift`, add `public struct MathAccent: MathComponent` with `base: [MathComponent]` and `accentChar: String`, implementing `toOMML()` so MathAccent emits valid OMML accent element per ECMA-376 §22.1.2.1; bump `Package.swift` to `0.11.0` and add a `## 0.11.0` entry to `CHANGELOG.md` confirming ooxml-swift package version reflects MathAccent addition as additive non-breaking
- [x] 1.2 [P] Add `Tests/OOXMLSwiftTests/MathComponentTests.swift` cases for hat over single run, bar over Greek letter, accent over composite SubSuperScript base, and accent character requiring XML escape — establishing baseline coverage for the Decision: Add `MathAccent` to `ooxml-swift`, bump v0.10.0 → v0.11.0
- [x] 1.3 Run `swift test` for ooxml-swift, then commit, push, tag `v0.11.0`, and `gh release create v0.11.0` — completes Decision: Build sequence requires three ordered releases; no cross-repo PR coupling step 1

## 2. latex-math-swift v0.1.0 — New parser package

- [x] 2.1 Create new GitHub repo `PsychQuant/latex-math-swift`, scaffold `Package.swift` declaring single dep `https://github.com/PsychQuant/ooxml-swift.git from: "0.11.0"`, library product `LaTeXMathSwift`, test target `LaTeXMathSwiftTests`, plus README/LICENSE — establishes Decision: Extract parser as new `latex-math-swift` package with pure-parser boundary and satisfies LaTeXMathParser depends only on OOXMLSwift
- [x] 2.2 [P] Implement `Sources/LaTeXMathSwift/TokenDictionary.swift` with `[String: String]` tables for: lowercase Greek (`\alpha`-`\omega`), uppercase Greek (`\Gamma`-`\Omega`), variants (`\varepsilon`/`\vartheta`/`\varphi`/`\varpi`/`\varrho`/`\varsigma`), and operators (`\cdot`/`\times`/`\pm`/`\mp`/`\sim`/`\approx`/`\neq`/`\le`/`\leq`/`\ge`/`\geq`/`\to`/`\rightarrow`/`\infty`/`\partial`/`\nabla`/`\cdots`/`\ldots`/`\mid`/`\quad`/`\,`)
- [x] 2.3 [P] Implement `Sources/LaTeXMathSwift/LaTeXParseError.swift` with cases `.empty`, `.unrecognizedToken(token: String)`, `.malformed(message: String)` — backs LaTeXMathParser throws structured errors naming the unsupported token
- [x] 2.4 Implement `Sources/LaTeXMathSwift/MacroDispatcher.swift` recursive-descent core covering `\frac{a}{b}` → `MathFraction`, `\sqrt{a}` and `\sqrt[n]{a}` → `MathRadical`, accent macros `\hat`/`\bar`/`\tilde`/`\dot`/`\overline` → `MathAccent` with combining diacritics, and structural sub/superscript `_{...}`/`^{...}` (both orderings normalized) → `MathSubSuperScript` — first half of LaTeXMathParser supports the expanded macro coverage required by issue #22
- [x] 2.5 Extend `MacroDispatcher.swift` with `\left(...\right)` / `\left[...\right]` / `\left\{...\right\}` / `\left|...\right|` / `\left\|...\right\|` → `MathDelimiter`; `\sum_{a}^{b}` / `\int_{a}^{b}` / `\prod_{a}^{b}` (with or without bounds) → `MathNary`; function names `\ln`/`\sin`/`\cos`/`\tan`/`\log`/`\exp`/`\max`/`\min`/`\det` followed by parenthesized argument → `MathFunction`; limits `\sup_{x}` / `\inf_{x}` / `\lim_{x \to 0}` → `MathLimit`; `\text{...}` → `MathRun(style: .plain)` — completes LaTeXMathParser supports the expanded macro coverage required by issue #22
- [x] 2.6 Add `Sources/LaTeXMathSwift/LaTeXMathParser.swift` exposing exactly one public static method `parse(_ latex: String) throws -> [MathComponent]` that delegates to MacroDispatcher and refuses to emit any wrapper XML — satisfies LaTeXMathParser exposes a single public parse entry point and the pure-parser boundary clause of the package extraction decision
- [x] 2.7 Add `Tests/LaTeXMathSwiftTests/` covering all 18 issue-22 fixture equations + sup-first vs sub-first normalization + 5-level nested-fraction recursion-depth stress test + each `LaTeXParseError` case + delimiter mismatch — first half of Decision: Golden test suite asserts OMML XML strings + round-trips through `OMMLParser` (parser-only assertions, no docx round-trip yet)
- [x] 2.8 Run `swift test` for latex-math-swift, commit, push, tag `v0.1.0`, and `gh release create v0.1.0` — completes Decision: Build sequence requires three ordered releases; no cross-repo PR coupling step 2

## 3. che-word-mcp v3.2.0 — Integrate parser + ship

- [x] 3.1 In `mcp/che-word-mcp/Package.swift`, bump `ooxml-swift` dep to `^0.11.0` and add new dep `latex-math-swift ^0.1.0`; in `Sources/CheWordMCP/Server.swift`, delete lines 7155-7371 (`parseLatexSubset` / `parseLatexPlain` / `parseFracTopLevel` / `parseSingleBracedArg` / `scanBalancedBraceBody`) and replace with `import LaTeXMathSwift` + a `parseLatex(_ latex: String) throws -> [MathComponent]` thin wrapper calling `LaTeXMathParser.parse()`; preserve all OMML wrapper code (`<m:oMathPara>` / `<m:oMath>`) in lines 7090-7130 — implements Decision: Replace `parseLatexSubset` with `LaTeXMathParser.parse()` delegation; preserve all surrounding `Server.swift` equation-insert code
- [x] 3.2 Rewrite the `latex.description` schema string at `Server.swift:2153`, the swiftdoc comment at `Server.swift:7097-7099`, and the `mcpb/manifest.json` tool description for `insert_equation`, enumerating supported macro families under 800 chars and ending with `components:` fallback guidance — satisfies insert_equation latex fallback supports documented subset
- [x] 3.3 Add `Tests/CheWordMCPTests/InsertEquationGoldenTests.swift` containing all 18 issue-22 fixture equations with expected OMML XML strings (whitespace-normalized assertion) plus `.docx` round-trip through `ooxml-swift`'s `OMMLParser` asserting AST equality, plus a file-header comment block documenting the manual MS Word "double-click → native equation editor" verification matrix — completes Decision: Golden test suite asserts OMML XML strings + round-trips through `OMMLParser` end-to-end
- [x] 3.4 Update `mcp/che-word-mcp/CHANGELOG.md` with a `## 3.2.0` entry flagging the parser delegation and behavior expansion (equations that previously errored now succeed), bump `mcpb/manifest.json` version field, run `swift test`, commit, push, tag `v3.2.0`, `gh release create v3.2.0` and upload `mcpb/che-word-mcp.mcpb` — completes Decision: Build sequence requires three ordered releases; no cross-repo PR coupling step 3 and closes `PsychQuant/che-word-mcp#22`
