## 0. Dependency coordination (added during apply, per session decision)

- [x] 0.1 Update `packages/word-builder-swift/Package.swift` to branch-track `ooxml-swift` main instead of pinning to a version. Rationale: this Spectra change consumes Edit-algebra runtime + Phase 2c reducer cases that landed on ooxml-swift main but post-date the latest tag (v0.31.4). Per user direction during apply ("不要pin，一直都使用最新的，依然要用下載而不是用local是因為要部署用"), use `.package(url:, branch: "main")` to always pull the latest main, preserving git-remote deployment semantics. **Behavior**: `swift package update` resolves ooxml-swift to current `main` HEAD; `Package.resolved` records a branch+revision state rather than a version+revision state. **Verify**: `cat Package.resolved | python3 -c "import json,sys; print([p['state'] for p in json.load(sys.stdin)['pins'] if 'ooxml' in p['identity']])"` shows `{'branch': 'main', 'revision': '<sha>'}` form. ✓ Completed at session start; resolved to revision `ee6c65c`.

## 1. Implement LensDocument core

- [x] 1.1 Create `Sources/WordBuilderSwift/LensDocument.swift` exposing `public struct LensDocument` with the five-method surface defined in spec.md Requirement "LensDocument exposes a five-method authoring surface" + Requirement "LensDocument is the top-level document handle". Implements design.md "Decision 2: `LensDocument` wraps `OOXMLSwift.WordDocument`; does NOT replace it". The private `inner: OOXMLSwift.WordDocument` field carries state. **Behavior**: callers writing `let doc = LensDocument()` get a value-type document handle; subsequent `try doc.apply(someEdit)` returns a new instance with the Edit applied. **Verify**: `swift test --filter "WordBuilderSwiftTests.testLensDocumentEmptyInit"` (test added in §3.1) passes.

- [x] 1.2 [P] Rewrite `Sources/WordBuilderSwift/WordBuilderSwift.swift` to be `@_exported import OOXMLSwift`. Covers spec.md Requirement "WordBuilderSwift module re-exports the OOXMLSwift Edit-algebra surface". Implements design.md "Decision 3: Reuse ooxml-swift's `Edit` protocol; no `WordBuilderEdit`". **Behavior**: `import WordBuilderSwift` surfaces `Edit`, `OOXMLEdit`, `WordEdit`, `EditError`, `WordRange`, `ParagraphRef` etc. without a second import. **Verify**: a smoke test (`testReExportsEditProtocol`, §3.2) references `Edit.self` after `import WordBuilderSwift` alone and compiles.

## 2. Remove v0.9.0 surface

Implements design.md "Decision 1: Clean break in v1.0.0, no coexistence cycle" + "Decision 6: `Packer` and `DocumentConverter` are removed wholesale, not refactored". Collectively removes all 11 v0.9.0 Requirements per the spec delta's REMOVED block.

- [x] 2.1 [P] Remove `Sources/WordBuilderSwift/Document.swift` (`Document` and `Section` structs). Covers REMOVED Requirements "Document accepts a sections array and Phase 1 emits only the first section" + "Public API mirrors docx.js top-level types" (Document portion). **Behavior**: `Document(sections:)` no longer compiles. **Verify**: `swift build` succeeds after removal; `grep -rn "struct Document" Sources/WordBuilderSwift` returns 0 results; `grep -rn "struct Section" Sources/WordBuilderSwift` returns 0 results.

- [x] 2.2 [P] Remove `Sources/WordBuilderSwift/Packer.swift` (the `Packer` enum and its three static methods). Covers REMOVED Requirement "Packer provides toData, toFile, and toBase64String static methods" + design.md Decision 6. **Behavior**: `Packer.toFile`, `Packer.toData`, `Packer.toBase64String` no longer compile. **Verify**: `swift build` succeeds; `grep -rn "enum Packer" Sources/WordBuilderSwift` returns 0 results.

- [x] 2.3 [P] Remove `Sources/WordBuilderSwift/DocumentConverter.swift` (the v0.9.0 → ooxml-swift bridge). Covers design.md Decision 6 (DocumentConverter removed wholesale). **Behavior**: nothing in word-builder-swift's public surface depends on DocumentConverter after §1.1 ships. **Verify**: `swift build` succeeds; `grep -rn "DocumentConverter" Sources/WordBuilderSwift` returns 0 results.

- [x] 2.4 [P] Remove the word-builder-swift-local `Paragraph`, `Run`, `Table` types — `Sources/WordBuilderSwift/Paragraph.swift`, `Run.swift`, `Table.swift`. Covers REMOVED Requirements "TextRun accepts a bare String and an options-style init", "Paragraph accepts heading, alignment, and children as init parameters with defaults", "Table accepts rows of cells and renders to OOXML table" + the Run/Table portion of "Public API mirrors docx.js top-level types". **Behavior**: `WordBuilderSwift.Paragraph` is no longer a distinct type; callers using `Paragraph` get OOXMLSwift's type via the §1.2 re-export. **Verify**: `swift build` succeeds; the symbol `WordBuilderSwift.Paragraph` (as distinct from `OOXMLSwift.Paragraph`) no longer exists per `swift package describe --type json | jq '.targets[] | select(.name == "WordBuilderSwift") | .symbols // "ok"'`.

- [x] 2.5 [P] Remove `Sources/WordBuilderSwift/Enums.swift` (HeadingLevel and AlignmentType enums). Covers REMOVED Requirement "HeadingLevel and AlignmentType enums mirror docx.js values". **Behavior**: `HeadingLevel.heading1` and `AlignmentType.center` no longer compile from `import WordBuilderSwift`. **Verify**: `swift build` succeeds; `grep -rn "enum HeadingLevel\|enum AlignmentType" Sources/WordBuilderSwift` returns 0 results.

## 3. Tests (covers spec.md Requirement "LensDocument exposes a five-method authoring surface" + "Edit-protocol failure modes propagate unchanged")

- [x] 3.1 Add `Tests/WordBuilderSwiftTests/LensDocumentTests.swift` containing `testLensDocumentEmptyInit` (verifies `LensDocument()` succeeds, the doc's emit produces a non-empty `.docx`) and `testLensDocumentReadingURL` (writes a fixture file then reads it via `LensDocument(reading:)`, verifies subsequent emit produces the same content). Tests spec.md Requirement "LensDocument is the top-level document handle" scenarios. **Behavior**: LensDocument construction round-trips through `OOXMLSwift.DocxReader`/`DocxWriter`. **Verify**: both tests pass under `swift test --filter LensDocumentTests`.

- [x] 3.2 Add `Tests/WordBuilderSwiftTests/ReExportTests.swift` containing `testReExportsEditProtocol` that declares `let _: Edit.Type = OOXMLEdit.self` after `import WordBuilderSwift` only (no `import OOXMLSwift`). Tests spec.md Requirement "WordBuilderSwift module re-exports the OOXMLSwift Edit-algebra surface" scenarios. **Behavior**: re-export brings the Edit protocol into the WordBuilderSwift namespace. **Verify**: test compiles and passes.

- [x] 3.3 Add `Tests/WordBuilderSwiftTests/ApplyTests.swift` containing `testApplySingleEdit` (constructs a fixture LensDocument, applies one `OOXMLEdit.insertParagraph`, verifies the resulting LensDocument's emit-then-re-read shows the inserted paragraph), `testApplySequence` (chains two Edits), `testApplyImmutability` (asserts the input doc is unchanged after apply), and `testApplyPropagatesNotImplemented` (asserts EditError from a stub case propagates per spec.md Requirement "Edit-protocol failure modes propagate unchanged"). **Behavior**: matches the four apply-related scenarios in spec.md plus the failure-mode scenarios. **Verify**: all four tests pass under `swift test --filter ApplyTests`.

- [x] 3.4 Add `Tests/WordBuilderSwiftTests/EmitTests.swift` containing `testEmitOverwritesExistingFile` (creates a file at the URL, calls emit, verifies the bytes match the new emit not the old file) and `testEmitToFreshURL` (verifies a fresh URL gets written). Tests spec.md Requirement "LensDocument exposes a five-method authoring surface" emit scenarios. **Behavior**: matches the two `emit(to:)` scenarios in spec.md. **Verify**: both tests pass under `swift test --filter EmitTests`.

- [x] 3.5 Delete the v0.9.0 test files under `Tests/WordBuilderSwiftTests/` whose names match patterns `*Document*Tests.swift`, `*Section*Tests.swift`, `*Paragraph*Tests.swift`, `*Run*Tests.swift`, `*Table*Tests.swift`, `*Packer*Tests.swift`, `*DocumentConverter*Tests.swift`, `*HeadingLevel*Tests.swift`, `*AlignmentType*Tests.swift`. These tests asserted the v0.9.0 spec Requirements being REMOVED (the 11 REMOVED Requirements collectively). **Behavior**: no test files reference removed types. **Verify**: `swift test` succeeds with no compile errors (so no orphan test files remain referencing deleted types); `grep -rln "Document(sections:" Tests/ Sources/ examples/` returns 0 results.

## 4. Examples rewrite

Implements design.md "Decision 4: Examples are rewritten in the same PR; file numbering preserved". Covers spec.md Requirement "Examples demonstrate the lens-model authoring flow". Replaces REMOVED Requirement "Phase 1 ships five worked examples translated from docx.js README".

- [x] 4.1 Rewrite `examples/01-hello-world.swift` to construct a LensDocument and apply a single `WordEdit.applyInsertParagraph(after: ParagraphRef, content: "Hello, World!")` then `emit(to: url)`. Covers spec.md "Examples demonstrate the lens-model authoring flow" hello-world scenario. **Behavior**: the file is a runnable Swift program demonstrating the simplest lens-model usage. **Verify**: `swift build` of the package succeeds; if the package wires examples as a build target, that build also succeeds.

- [x] 4.2 [P] Rewrite `examples/02-heading-and-paragraphs.swift` to chain three `applyInsertParagraph` calls and set Heading1 style on the first via `OOXMLEdit.setParagraphStyle`. **Behavior**: file compiles and demonstrates the apply-sequence pattern. **Verify**: same as 4.1.

- [x] 4.3 [P] Rewrite `examples/03-table-3x3.swift` to demonstrate the call shape for `OOXMLEdit.insertTable` and `setCellText`. Covers spec.md "Examples that exercise Phase-2c-pending Edits document the gap" scenario. Because Phase 2c Reducer support for tables hasn't shipped yet (ooxml-swift#71), the example wraps the calls in `try?` with an explanatory comment naming `ooxml-swift#71`. **Behavior**: file compiles and shows the intended call shape, but acknowledges runtime gap. **Verify**: file compiles under `swift build`; manual inspection confirms the explanatory comment references ooxml-swift#71.

- [x] 4.4 [P] Rewrite `examples/04-mixed-formatting.swift` to chain `applyInsertParagraph` then `WordEdit.applyBold(range:)`. **Behavior**: demonstrates the formatting-on-existing-text pattern. **Verify**: same as 4.1.

- [x] 4.5 [P] Rewrite `examples/05-aligned-paragraphs.swift` to chain `applyInsertParagraph` then `OOXMLEdit.setParagraphStyle(target:, styleId:)` (alignment via style ID, since dedicated alignment Edit cases are tracked in ooxml-swift#71 follow-ups). **Behavior**: demonstrates the style-via-Edit pattern. **Verify**: same as 4.1.

## 5. Documentation and packaging

Implements design.md "Decision 5: README "Migration from 0.9.0" is a 3-row table, not a paragraph essay". Covers spec.md Requirement "README documents the migration and architecture". Replaces REMOVED Requirements "Version number mirrors docx.js minor version", "reference docx-js is read-only, no code copying", "OOXMLSwift.DocxWriter exposes writeData for in-memory output".

- [x] 5.1 Update `packages/word-builder-swift/Package.swift` to declare version 1.0.0 (replaces REMOVED Requirement "Version number mirrors docx.js minor version"). **Behavior**: `swift package show-dependencies` against a consumer that pins `.exact("1.0.0")` resolves to this revision. **Verify**: manual inspection of `Package.swift` shows the 1.0.0 version reference.

- [x] 5.2 [P] Rewrite `packages/word-builder-swift/README.md` adding two top-level sections: "Migration from 0.9.0" (with the 3-row table from design.md Decision 5) and "Architecture" (citing OOXMLSwift.WordDocument as the wrapped type, the Edit protocol as the authoring vocabulary, and ooxml-swift#71 as the Phase 2c Reducer-gap tracker). Covers spec.md "README documents the migration and architecture" Requirement scenarios (migration table + architecture section). Replaces the existing v0.9.0-oriented top sections (which referenced REMOVED Requirements "reference docx-js is read-only, no code copying" + "OOXMLSwift.DocxWriter exposes writeData for in-memory output"). **Behavior**: a reader opening README.md sees the v1.0.0 lens-model overview at the top, with migration guidance for v0.9.0 callers (none currently exist but the spec change requires the documentation). **Verify**: `grep -A 5 "## Migration from 0.9.0" README.md` returns the 3-row table; `grep -A 3 "## Architecture" README.md` returns the section header followed by content citing OOXMLSwift.WordDocument.

- [x] 5.3 [P] Create or update `packages/word-builder-swift/CHANGELOG.md` with a v1.0.0 entry describing the BREAKING change and naming the foundation references (macdoc#101, macdoc#99 ADR-008, this Spectra change's archive path after archival). If CHANGELOG.md doesn't exist, create it with v1.0.0 as the first entry. **Behavior**: a release engineer running `cat CHANGELOG.md | head -20` sees the v1.0.0 entry with sufficient context to author the GitHub release notes. **Verify**: `head -30 packages/word-builder-swift/CHANGELOG.md` shows the v1.0.0 entry.

## 6. Validation and cleanup

- [x] 6.1 Run `swift build` and `swift test` in `packages/word-builder-swift/`. **Behavior**: the package compiles with no warnings (specific to this change's code; pre-existing unrelated warnings are documented but not blocked) and all new tests pass. **Verify**: `cd packages/word-builder-swift && swift test` exits 0 with no failures.

- [x] 6.2 Run `spectra validate word-builder-swift-lens-migration`. **Behavior**: the Spectra change passes structural and content validation. **Verify**: command exits 0 with "✓ ... — valid" message.

- [x] 6.3 Smoke-test downstream packages that import word-builder-swift to confirm no new transitive build breakage. Per the scout finding (zero current importers), this step expects no breakage but documents the check. **Behavior**: no consumer package picks up word-builder-swift via its Package.swift dependency graph, so a smoke-test command `find . -name 'Package.swift' -exec grep -l 'word-builder-swift\|WordBuilderSwift' {} \;` returns 0 results. **Verify**: command returns no matches.
