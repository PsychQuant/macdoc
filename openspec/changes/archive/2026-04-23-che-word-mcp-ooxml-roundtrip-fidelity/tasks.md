## 1. Phase 1 — ooxml-swift v0.12.0 foundation (round-trip fidelity)

- [x] 1.1 Create `packages/ooxml-swift/Sources/OOXMLSwift/IO/PreservedArchive.swift` defining `struct PreservedArchive { let tempDir: URL; func cleanup() }`; add `archiveTempDir: URL?` private property to `WordDocument` in `packages/ooxml-swift/Sources/OOXMLSwift/Models/Document.swift` and a `public mutating func close()` that calls `ZipHelper.cleanup(archiveTempDir!)` and sets the property to nil — implements Decision: PreservedArchive is a tempDir-backed reference, not an in-memory `[String: Data]` + Decision: WordDocument.close() is explicit; no automatic cleanup and satisfies "WordDocument retains source archive tempDir for round-trip preservation" + "WordDocument.close() releases the archive tempDir" + "DocxReader retains source unzip tempDir on the returned WordDocument"
- [x] 1.2 Modify `packages/ooxml-swift/Sources/OOXMLSwift/IO/DocxReader.swift` `read(from:)` to remove the `defer { ZipHelper.cleanup(tempDir) }` and instead set `result.archiveTempDir = tempDir` before returning — completes "DocxReader retains source unzip tempDir on the returned WordDocument" and the second scenario of "WordDocument retains source archive tempDir for round-trip preservation"
- [x] 1.3 [P] Create `packages/ooxml-swift/Sources/OOXMLSwift/IO/RelationshipIdAllocator.swift` with `class RelationshipIdAllocator` exposing `init(originalRelsXML: String, typedFields: ...)`, `func allocate() -> String`, `func reserve(_ id: String)`; scan original rels XML with regex for `Id="rId(\d+)"` integers, compute `nextId = max + 1`; gracefully skip non-numeric rId suffixes — implements Decision: RelationshipIdAllocator scans original rels and types together + satisfies "RelationshipIdAllocator scans original rels and typed fields together" and "RelationshipIdAllocator generates collision-free rIds across preserved and typed relationships"
- [x] 1.4 [P] Create `packages/ooxml-swift/Sources/OOXMLSwift/IO/ContentTypesOverlay.swift` with `struct ContentTypesOverlay` exposing `init(originalContentTypesXML: String)` and `func merge(typedParts: [PartDescriptor]) -> String` implementing the typed-overrides-precedence + preserve-unknown-overrides + add-new-overrides algorithm for both `<Override>` and `<Default>` entries — implements Decision: Content_Types.xml overlay merge — preserve unknown Overrides, dedupe typed Overrides + satisfies "ContentTypesOverlay merges typed parts with preserved Override entries" and "ContentTypesOverlay merge produces a single Override per PartName"
- [x] 1.5 Modify `packages/ooxml-swift/Sources/OOXMLSwift/IO/DocxWriter.swift` `write(_ document: WordDocument, to dest: URL)` to detect `document.archiveTempDir != nil`; in overlay mode overwrite typed-model parts directly into archiveTempDir (replacing the prior scratch-tempDir build), use `RelationshipIdAllocator` for new rId generation (replacing the naive counter at line 238), use `ContentTypesOverlay.merge` for `[Content_Types].xml`, then `ZipHelper.zip(archiveTempDir, to: dest)`; preserve scratch mode unchanged for documents with `archiveTempDir == nil` — implements Decision: DocxWriter has two modes — overlay (when archiveTempDir set) and scratch (when nil) + satisfies "DocxWriter overlay mode preserves unknown OOXML parts byte-for-byte" + "DocxWriter detects archiveTempDir and switches to overlay mode" + "Container coverage extends to opaque preservation of all unknown parts"
- [x] 1.6 Hand-craft `packages/ooxml-swift/Tests/OOXMLSwiftTests/Fixtures/minimal-multipart.docx` (~30KB) via building it programmatically once with Phase-1 writer in scratch mode, then committing the binary as a fixture; document the build procedure in `Tests/OOXMLSwiftTests/Fixtures/README.md`; fixture MUST contain 1 body paragraph + 1 default header + 1 default footer + theme1.xml with `minorEastAsia` font set + 1 `<w15:person>` in people.xml + webSettings.xml setting `relyOnVML=true` + 1 image in media/ — implements Decision: Regression test fixture is a hand-crafted minimal multipart docx, not the NTPU thesis
- [x] 1.7 Create `packages/ooxml-swift/Tests/OOXMLSwiftTests/RoundTripFidelityTests.swift` with at least: (a) entry-list-equality test, (b) byte-equality tests for theme1.xml / people.xml / webSettings.xml, (c) Content_Types Override-set equality test, (d) RelationshipIdAllocator collision-free test (preserved rId99 → next allocate returns rId100 or higher), (e) ContentTypesOverlay test (preserved theme + replaced document + new image scenarios), (f) WordDocument.close() idempotency test — satisfies all 4 scenarios of "Round-trip fidelity regression test asserts byte-equality on minimal-multipart fixture" plus the close()-idempotency scenario
- [x] 1.8 Update `packages/ooxml-swift/CHANGELOG.md` with v0.12.0 entry flagging the architectural shift (preserve-by-default), the new public API (`WordDocument.close()`), the new helper types (`PreservedArchive`, `RelationshipIdAllocator`, `ContentTypesOverlay`), and the BREAKING semantic change (callers must call `close()` to free tempDirs); run `swift test` to confirm all 310+ existing tests still pass plus the new RoundTripFidelityTests; commit, push, tag `v0.12.0`, `gh release create v0.12.0` — completes Decision: PreservedArchive + closes Phase 1 of the build sequence

## 2. Phase 2A — che-word-mcp v3.3.0 (theme + headers/footers tools, NTPU thesis fix)

- [x] 2.1 Bump `mcp/che-word-mcp/Package.swift` `ooxml-swift` dep to `^0.12.0`, run `swift package update`; in `mcp/che-word-mcp/Sources/CheWordMCP/Server.swift` modify the existing `close_document` MCP tool handler to call `WordDocument.close()` on the released document, and modify session shutdown logic to call `close()` on every still-open document — wires Phase 1's WordDocument.close() into che-word-mcp's session lifecycle
- [x] 2.2 Implement theme tools in `mcp/che-word-mcp/Sources/CheWordMCP/Server.swift`: `get_theme`, `update_theme_fonts`, `update_theme_color`, `set_theme` — parsing `word/theme/theme1.xml` from `archiveTempDir`, mutating in place, validating hex color format and slot names; add `Tests/CheWordMCPTests/ThemeToolsTests.swift` covering all 8 scenarios from the theme-tools spec — satisfies "get_theme MCP tool returns parsed theme structure" + "update_theme_fonts MCP tool partially updates major and minor font slots" + "update_theme_color MCP tool replaces a single color slot" + "set_theme MCP tool replaces theme1.xml verbatim"
- [x] 2.3 Implement headers tools in `mcp/che-word-mcp/Sources/CheWordMCP/Server.swift`: `list_headers`, `get_header`, `delete_header`, `list_watermarks`, `get_watermark` — including watermark VML detection (`PowerPlusWaterMarkObject` shape ID + `o:spt="136"` sentinel), section-properties cleanup on delete, and watermark text/image parameter parsing; add `Tests/CheWordMCPTests/HeadersFootersToolsTests.swift` covering header scenarios — satisfies "list_headers MCP tool enumerates all header parts in document order" + "get_header MCP tool returns text content and full XML for a header part" + "delete_header MCP tool removes header part and all references" + "list_watermarks MCP tool enumerates watermark VML shapes across all header parts" + "get_watermark MCP tool returns full parameters for a header's watermark"
- [x] 2.4 Implement footers tools in `mcp/che-word-mcp/Sources/CheWordMCP/Server.swift`: `list_footers`, `get_footer`, `delete_footer` — including PAGE/NUMPAGES field detection from `<w:fldSimple>` and `<w:instrText>`; add footer scenarios to `Tests/CheWordMCPTests/HeadersFootersToolsTests.swift` — satisfies "list_footers MCP tool enumerates all footer parts in document order" + "get_footer MCP tool returns text content, full XML, and field structure" + "delete_footer MCP tool removes footer part and all references"
- [x] 2.5 Modify existing `add_header`, `update_header`, `add_footer`, `update_footer` MCP tools in `mcp/che-word-mcp/Sources/CheWordMCP/Server.swift` to use `RelationshipIdAllocator.allocate()` instead of the prior naive count-based rId generation; ensure update tools preserve original part filename and rId (in-place file overwrite in archiveTempDir); add regression scenarios to `Tests/CheWordMCPTests/HeadersFootersToolsTests.swift` — satisfies "add_header and add_footer use RelationshipIdAllocator for collision-free rIds" + "update_header and update_footer preserve original part filename"
- [x] 2.6 Update `mcp/che-word-mcp/CHANGELOG.md` with v3.3.0 entry flagging the 12 new MCP tools, the modified `add_header`/`update_header`/`add_footer`/`update_footer` contracts, the dep bump to ooxml-swift 0.12.0, and the round-trip fidelity behavior change (closes #23 #26 #27 #28); bump `mcpb/manifest.json` version field; run `swift test`; commit, push, tag `v3.3.0`, `gh release create v3.2.0`, build .mcpb and upload — completes Phase 2A of the build sequence + Decision: Phase 2 ships in 3 batches by SDD value, not all at once (Batch 2A)

## 3. Phase 2B — che-word-mcp v3.4.0 (comment threads + people tools)

- [x] 3.1 Implement comment thread tools in `mcp/che-word-mcp/Sources/CheWordMCP/Server.swift`: `list_comment_threads`, `get_comment_thread`, `sync_extended_comments` — parsing `<w15:commentEx>` parent/child relationships from `commentsExtended.xml`, building tree structures, generating UUID-based durable IDs for `commentsIds.xml` sync; add `Tests/CheWordMCPTests/CommentThreadsToolsTests.swift` covering all 5 scenarios — satisfies "list_comment_threads MCP tool returns parent-child comment hierarchy" + "get_comment_thread MCP tool returns full tree for a root comment" + "sync_extended_comments MCP tool repairs comments triplet consistency"
- [x] 3.2 Implement people tools in `mcp/che-word-mcp/Sources/CheWordMCP/Server.swift`: `list_people`, `add_person`, `update_person`, `delete_person` — including auto-creation of `people.xml` (with Content_Types Override + Relationship via RelationshipIdAllocator) when the part doesn't yet exist, duplicate-name handling with numeric suffix, and orphan-comment counting on delete; add `Tests/CheWordMCPTests/PeopleToolsTests.swift` covering all 5 scenarios — satisfies "list_people MCP tool returns all comment author records from people.xml" + "add_person MCP tool creates a new person record" + "update_person MCP tool partially updates a person record" + "delete_person MCP tool removes a person record"
- [x] 3.3 Modify existing `insert_comment`, `reply_to_comment`, `resolve_comment`, `delete_comment` MCP tools in `mcp/che-word-mcp/Sources/CheWordMCP/Server.swift` to auto-sync the four-part comment metadata (`commentsExtended.xml`, `commentsExtensible.xml`, `commentsIds.xml`, `people.xml`); for `resolve_comment` add the missing-extended sync-hint error; for `reply_to_comment` write `<w15:parentCommentId>` to commentsExtended; add scenarios to `Tests/CheWordMCPTests/CommentThreadsToolsTests.swift` — satisfies "insert_comment auto-syncs commentsExtended, commentsExtensible, commentsIds, and people.xml" + "reply_to_comment writes parent reference to commentsExtended" + "resolve_comment sets done flag in commentsExtended" + "delete_comment removes entries from all four comment-related parts"
- [x] 3.4 Update `mcp/che-word-mcp/CHANGELOG.md` with v3.4.0 entry flagging the 7 new MCP tools, the modified `insert_comment`/`reply_to_comment`/`resolve_comment`/`delete_comment` contracts (auto-sync 4 parts), and closes #29 #30; bump `mcpb/manifest.json`; run `swift test`; commit, push, tag `v3.4.0`, `gh release create v3.4.0`, build .mcpb and upload — completes Phase 2B of the build sequence + Decision: Phase 2 ships in 3 batches by SDD value, not all at once (Batch 2B)

## 4. Phase 2C — che-word-mcp v3.5.0 (notes update + web settings)

- [x] 4.1 [P] Implement notes update tools in `mcp/che-word-mcp/Sources/CheWordMCP/Server.swift`: `get_endnote`, `update_endnote`, `get_footnote`, `update_footnote` — preserving endnote/footnote IDs across update so cross-references in `document.xml` stay valid; returning runs array with bold/italic/color metadata for inspection; add `Tests/CheWordMCPTests/NotesUpdateToolsTests.swift` covering all 4 scenarios — satisfies "get_endnote MCP tool returns endnote text and runs by ID" + "update_endnote MCP tool replaces endnote text in place preserving ID" + "get_footnote MCP tool returns footnote text and runs by ID" + "update_footnote MCP tool replaces footnote text in place preserving ID"
- [x] 4.2 [P] Implement web settings tools in `mcp/che-word-mcp/Sources/CheWordMCP/Server.swift`: `get_web_settings`, `update_web_settings` — partial update by key (only specified fields modified), auto-creation of `webSettings.xml` (with Content_Types Override + Relationship) when absent; add `Tests/CheWordMCPTests/WebSettingsToolsTests.swift` covering all 4 scenarios — satisfies "get_web_settings MCP tool returns parsed webSettings.xml" + "update_web_settings MCP tool partially updates webSettings.xml"
- [x] 4.3 Update `mcp/che-word-mcp/CHANGELOG.md` with v3.5.0 entry flagging the 6 new MCP tools and closes #24 #25 #31; bump `mcpb/manifest.json`; run `swift test`; commit, push, tag `v3.5.0`, `gh release create v3.5.0`, build .mcpb and upload — completes Phase 2C of the build sequence + Decision: Phase 2 ships in 3 batches by SDD value, not all at once (Batch 2C); the entire `OOXML parts CRUD completeness` milestone is now complete and all 9 originating GitHub issues (#23-#31) ready for `/idd-verify` + `/idd-close`
